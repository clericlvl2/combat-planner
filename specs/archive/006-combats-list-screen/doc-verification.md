# Doc-verification: 006-combats-list-screen

Doc-sync pass. Checks the doc-syncer's edits to `specs/reference/component-inventory.md`,
`specs/reference/acceptance-matrix.md`, and the new `specs/CHANGELOG.md` row against what
`verification.md` (code pass, already PASS/PASS/PASS/PASS/PASS-override) says actually shipped.

| AC | Verdict | Evidence |
|----|---------|----------|
| `CHANGELOG.md` new row accurately summarizes what shipped, matches verification.md, no invented facts | PASS | `specs/CHANGELOG.md:15` claim-by-claim checked against `verification.md`: restyle list (CombatList/CombatRow/ColorTagDot/CombatRowMenu/EmptyState, CombatFormDialog, ConfirmDialog) matches verification.md's AC-1 evidence; "header-action.svelte.ts seam" + "desktop header create-button / mobile FAB split" matches verification.md's AC-1 evidence on the Phase-7 fix (`header-action.svelte.ts`, `AppHeader.svelte:68-72`, `+page.svelte:41-52,73,83`); "per-breakpoint nav (swipe-right sidebar mobile, burger tablet, inline icon-nav desktop)" matches verification.md's NavSidebar/AppHeader evidence; "Settings ... three inline `<section>` groups, not dedicated sub-components" and "no test file yet (flagged as a gap...)" matches verification.md's PASS-override reasoning verbatim (bare-stub pre-change, zero new test coverage). No fact in the row is unsupported by verification.md or the code. |
| `acceptance-matrix.md` SET-1/SET-2 row edits accurately reflect the shipped gap (no test file, layer downgrade) | PASS | Old rows claimed `C, E` layer coverage for SET-1/SET-2; verification.md's AC-4 evidence explicitly found "ships with zero new test coverage (no `settings/+page.svelte.spec.ts` exists), despite `specs/reference/acceptance-matrix.md` already listing SET-1/SET-2 as expected at 'C, E' layer" — i.e. verification.md flagged that claim as false. The new rows (`acceptance-matrix.md:14-15`) correct exactly this: downgrade to `— (planned C, E)` and add "**gap:** ... shipped (unit 006) with no test file yet". Confirmed no `src/routes/settings/+page.svelte.spec.ts` or `about/+page.svelte.spec.ts` exists (`find_files_by_glob` on `src/routes/settings/**` and `src/routes/about/**` return only the `+page.svelte` files). |
| `component-inventory.md` hierarchy/preamble edits accurately reflect shipped structure (no dedicated SettingsGroup/LanguageSwitcher/ThemeSwitcher/DataActions/AboutPage components; SearchBar still unshipped) | PASS | Confirmed via `find_files_by_name_keyword` for `SettingsGroup`, `LanguageSwitcher`, `ThemeSwitcher`, `AboutPage`, `DataActions`, `SearchBar` — zero files for all six. Read `src/routes/settings/+page.svelte`: three inline `<section class="rounded-lg border ...">` blocks (Language/Appearance/Data) plus an `<a href="/about">` link row, exactly as the new hierarchy text describes (`component-inventory.md:66-71`). Read `src/routes/about/+page.svelte`: single inline block (app name/version/description/privacy note), matching the new About hierarchy line and the updated "bespoke (no primitive)" table row. `src/routes/combats/+page.svelte` confirmed to have no SearchBar import/element, matching the unchanged-but-reworded SearchBar note. |
| `component-inventory.md` component-role table edits (Select/RadioGroup/Card/Separator rows) match shipped code | PASS | `settings/+page.svelte` imports `Select`/`SelectContent`/`SelectItem`/`SelectTrigger` and `ToggleGroup`/`ToggleGroupItem` directly (no wrapper component) — matches the reworded Select and RadioGroup/ToggleGroup rows ("inline ... in `settings/+page.svelte` (no dedicated ... component)"). `Card` row's removal of `SettingsGroup` and the `Separator` row's outright removal both check out: no `Card`/`Separator` primitive is imported in `settings/+page.svelte` (borders are done with plain Tailwind classes on `<section>`), so neither leaf should be attributed to a nonexistent `SettingsGroup`. |
| Doc edits introduce no new contradictions with verification.md or with each other | FAIL | `acceptance-matrix.md` was only partially reconciled. The doc-syncer fixed the SET-1/SET-2 rows (removing the false "C, E" claim) but left two adjacent rows for the *same* Settings surface stale and now self-contradictory: `SET-3 | resetAll (keeps lang/theme, re-runs first-launch); DataActions | U, C` (line 42) and `SET-5 | AboutPage render | C` (line 43) still name components (`DataActions`, `AboutPage`) that `component-inventory.md`'s own edit in this same pass says "no dedicated ... leaf components ship" / "no dedicated AboutPage component ships" — and both still claim `C` (component-test) coverage, but no `settings/+page.svelte.spec.ts` or `about/+page.svelte.spec.ts` exists (confirmed above), which is exactly the same "no test file" gap the syncer just called out for SET-1/SET-2. This is an internal contradiction between the two reference docs edited in this pass (component-inventory.md says the components don't exist; acceptance-matrix.md still cites them as tested) and it understates the same coverage gap verification.md raised. |

## Scope check

In scope and consistent: `specs/reference/component-inventory.md`, `specs/reference/acceptance-matrix.md` (SET-1/SET-2 rows), `specs/CHANGELOG.md` — all three edits stay within reference/changelog facts for unit 006 and do not touch any capability spec (`specs/capabilities/settings.md`, `specs/capabilities/combats-list.md`, `specs/capabilities/platform.md` are untouched — confirmed via `git status`), matching the expectation that requirements text doesn't change, only the reference "what shipped" facts.

Two things outside the declared doc-syncer edit set, flagged for awareness (not attributed to this doc-sync pass, not re-verified here since they read as implementation-time additions rather than doc-sync output):
- `messages/en.json` (+`nav.open`/`nav.primary`) and `specs/reference/i18n-catalog.md` (same two keys) are modified in the working tree alongside the three declared files. These look like implementer-added i18n catalog entries for the new NavSidebar/AppHeader aria-labels (already covered by the code-pass `verification.md`), not new doc-syncer output — but they were not called out in the dispatch, so flagging for the record. Content itself is consistent (both keys added together, no mismatch).
- `specs/changes/006-combats-list-screen/change.md`'s frontmatter flipped `status: approved` → `status: verifying`. This is a lifecycle-metadata edit, not a doc-sync content edit, and doesn't misstate any fact — noted only because it wasn't in the declared doc-syncer file list either.

## Other findings

- The FAIL above (acceptance-matrix.md SET-3/SET-5 rows) is a completeness gap in the same file the syncer already edited for the identical underlying reason (no test coverage) — not a fabrication, but a missed follow-through that leaves the reference doc internally inconsistent right after this pass. Recommend a follow-up edit to `acceptance-matrix.md` lines 42-43 mirroring the SET-1/SET-2 treatment: drop/rename `DataActions` and `AboutPage`, and downgrade the `C` layer claim until a `settings/+page.svelte.spec.ts` / `about/+page.svelte.spec.ts` exists.
- No unrelated capability facts (HP, initiative, turns/rounds, conditions, undo/redo, combatants, import/export) were touched by any of the three edited files — diffs are scoped entirely to unit 006's surfaces (combats-list, settings, about, nav chrome).
