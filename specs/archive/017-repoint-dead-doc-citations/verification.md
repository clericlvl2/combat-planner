# Verification: 017-repoint-dead-doc-citations

| AC | Verdict | Evidence |
|----|---------|----------|
| Zero dead `§` citations remain under `src/` | PASS | `grep -rE '(UX\|Component Inventory\|Data Model\|Data\|Rules( & Glossary)?\|Test Plan\|Glossary) ?§' src/` → no matches (exit 1) |
| Each edited comment cites a live capability ID / existing reference file / has dead `§` cleanly dropped | PASS (user override) — the 6 README rows cite a bare capability prefix (`CBT`/`INI`/`HP`/`CND`/`CLS`), which is a valid live owner for a component-map row and permitted by AC-7's "capability ID… where relevant"; all inline code comments resolve to real numeric IDs/anchors. | Vast majority verified good — spot-checked CLS-2, CLS-1, IMP-3, IMP-5, CND-1, CBT-1, HP-6, HP-1, LOG-1, UND-6 etc. all resolve to real capability-file anchors (`specs/capabilities/*.md`), and `specs/reference/{limits,acceptance-matrix,component-inventory}.md` all exist on disk. BUT `src/lib/components/app/README.md` (new component-map table) cites bare namespace tags with no numeric suffix — `capability CLS` (line "CombatFormDialog.svelte ... — capability CLS", "ColorSwatchPicker.svelte ... — capability CLS"), `capability CBT` (CombatantForm.svelte row), `capability INI` (InitCell.svelte row), `capability HP` (HealthBar.svelte row), `capability CND` (ConditionPicker.svelte row) — none of these match the AC's own regex `/[A-Z]{2,4}-\d+/`, and they aren't `specs/reference/*.md` filenames either. AC7's looser "capability ID... where relevant" wording arguably permits the namespace-only form, but AC2 as literally written (regex match) is not satisfied by these six README rows. |
| Diff touches only comments/JSDoc/`describe`/`it` labels — no code, assertions, imports, behavior change | PASS | Reviewed every `+` line across the non-README diff (`git diff -- src/ ':!.../README.md'`, 1512-line diff) — every added line is a `/** */`, `//`, `<!-- -->`, or `describe(`/`it(` label string; no assertion/expect lines, no import lines, no control-flow lines touched. |
| Live `ADR-0xx` references left untouched | PASS | `git diff` shows ADR-002/006/008/011/012/013 citations preserved verbatim alongside the repointed `§`/dead-namespace text (e.g. `migrations.ts`: `(IMP-5 / ADR-013)`, `types.ts` JSDoc `(ADR-013)` unchanged; README `(ADR-006)`, `(ADR-012)`, `(ADR-008)`, `(ADR-011)`, `(ADR-004)` all present pre/post). |
| `npm run gate` passes | PASS (user override) — gate ran green in-session: `GATE_EXIT=0`, lint clean, check 0/0 (5447 files), 30 files / 133 tests passed, build + PWA precache OK (via `CHOKIDAR_USEPOLLING=true` to sidestep the machine inotify limit). The verifier's failure was environmental (sibling worktree `combat-planner-018` + `vite dev` saturating `fs.inotify.max_user_watches`), not the diff. | `lint` passed (2 pre-existing warnings in `specs/design/prototype.html`, unrelated to this diff, no errors). `check` passed (0 errors/warnings, 5447 files). `test:unit` (`vitest --run`) fails to start with `ENOSPC: System limit for number of file watchers reached` — traced to a concurrent `vite dev` process (pid 278150) and a `vitest --run` from a sibling worktree `combat-planner-018` (pid 292327) already consuming the machine's inotify watcher budget; not caused by this diff's content. Gate could not be completed to `build` step as a result. Re-run once other processes are stopped / `fs.inotify.max_user_watches` raised (needs sudo, unavailable in this session). |
| `NumpadSheet.svelte`'s `§7 HP ranges` comment repointed to `specs/reference/limits.md` | PASS | `src/lib/components/app/NumpadSheet.svelte:78` (diff): `if (entry.length >= 4) return; // ≤ 9999, covers the HP ranges in specs/reference/limits.md` |
| `README.md` zero bare `§N` / `UX & IA §` pointers; component-map lists only components that exist on disk, each row citing a live owner | PASS | `grep -nE '§\|UX & IA' src/lib/components/app/README.md` → no matches. Every `.svelte`/`.ts` filename in the rewritten table (`AppHeader`, `AppShell`, `ColorSwatchPicker`, `ColorTagDot`, `CombatFormDialog`, `CombatList`, `CombatRow`, `CombatRowMenu`, `CombatantForm`, `CombatantRow`, `ConditionIconList`, `ConditionPicker`, `ConfirmDialog`, `EmptyState`, `FAB`, `HealthBar`, `InitCell`, `NavSidebar`, `NumberField`, `NumpadSheet`, `SearchField`, `CombatHeader`, `controller.ts`, `labels.ts`, `header-action.svelte.ts`) confirmed present via `ls src/lib/components/app/`; no phantom entries (old table's `Toaster`, `InstallBanner`, `NavLink`, `ImportControl`, `IconButton`, `RoundCounterControl`, `EscalationDieControl`, `CombatOverflowMenu`, `CombatantList`, `StartBar`, `TypeBadge`, `InitEntry`, `HpCell`, `DefenseStats`, `TempHpField`, `NoteField`, `RowActions`, `HpSummaryHeader`, `EntryDisplay`, `DigitPad`, `CommitActions`, `HpLogSection`, `HpLogEntryRow`, `CombatantFormDialog`, `TypeSelect`, `SettingsGroup`, `LanguageSwitcher`, `ThemeSwitcher`, `DataActions`, `AboutPage` — all removed). Each row cites a `component-inventory.md#anchor` or `capability <NS>`/`specs/reference/limits.md` — see the AC2 finding above re: the six bare-namespace citations (a live owner in spirit, but not a numeric capability ID). |

## Scope check

In-scope. All 57 `src/**` files touched are comments/JSDoc/labels/README-only (per AC3 evidence). The 58th changed file, `specs/backlog.md`, is a 1-line B-016 row status flip (`idea` → `in-unit`) plus a pointer to this change unit's directory — standard change-unit lifecycle bookkeeping (tracking which backlog item this unit implements), not capability/reference/ADR content, so it does not violate the stated out-of-scope rule ("Editing any file under `specs/**` (capability files, reference docs, ADRs)" — `backlog.md` is none of those three). No file under `specs/capabilities/**`, `specs/reference/**`, or `specs/adr/**` was modified.

`specs/backlog.md` diff:
```diff
-| B-016 | ... | INFRA | S | idea | — |
+| B-016 | ... | INFRA | S | in-unit | `specs/changes/017-repoint-dead-doc-citations/` |
```

No executable-code, test-assertion, or import edits found anywhere in the reviewed diff (see AC3 evidence above) — no behavior-change regression risk identified in the reviewed lines.

## Other findings

- Gate could not be fully completed (`test:unit`/`build` steps blocked by a pre-existing environment resource exhaustion — concurrent `vite dev` + a sibling-worktree `vitest --run` saturating `fs.inotify.max_user_watches=65536`). This is not a defect introduced by this change unit; recommend re-running `npm run gate` in a clean environment (stop other dev/test processes, or raise the watcher limit) before final sign-off.
- No regressions or contradictions of unrelated capability ACs were spotted in the reviewed diff — all changes are comment/doc-pointer-only as intended.
