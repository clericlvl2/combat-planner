# Verification: 007-combat-screen

| AC | Verdict | Evidence |
|----|---------|----------|
| Setup/Active render styled to approved prototype (mobile+desktop, dark+light) | PASS | CombatHeader.svelte (header restyle, RoundEscBar sub-bar, `lg:` desktop icon pair), +page.svelte (`lg:max-w-3xl`, `lg:hidden` FAB stack) — all colors via tokens (bg-card, border-border, text-muted-foreground etc.), theme-agnostic |
| CombatantRow/CombatHeader/StartBar/FAB/JumpToTurnButton/NumpadSheet(+log)/CombatantFormDialog match template | PASS | CombatantRow.svelte (5-row card restructure per comment block), NumpadSheet.svelte (CommitActions moved above DigitPad, HpLogSection styling), CombatantForm.svelte (field-label recipe, 3-col grid), new src/lib/components/app/JumpToTurnButton.svelte, +page.svelte (FAB stack) |
| HealthBar shows 4 bands + active-turn highlight, token-driven | PASS | HealthBar.svelte:24 unchanged healthColor map (still token classes), height bumped h-1.5→h-2; CombatantRow.svelte:465 `active && 'ring-2 ring-primary'` retained for active-turn highlight |
| All color/spacing/type from layout.css tokens, no hard-coded hex/oklch | PASS | grep for `#[0-9a-fA-F]{3,6}` / `oklch(` across src/lib/components/app + src/routes/combats returned no real matches (only `{#each ... as c (c)}` syntax false positives) |
| PLT-5: visible focus, ≥44px targets, no color-alone | PASS | Widespread `min-h-11`/`min-w-11` (44px) on Button/Input/ToggleGroupItem/step buttons (NumberField.svelte:816,834; CombatHeader.svelte:76,140; ConditionPicker.svelte:711; CombatantForm.svelte:315); `focus-within:ring-3`/`focus-visible:ring-3` present (NumberField.svelte:814,778); TypeStripe aria-label unchanged (CombatantRow.svelte, stripeLabel still present); condition chips keep icon+text label (ConditionIconList.svelte) |
| No game-mechanics change — rules/state logic identical, existing tests pass | PASS | `npm run gate` (lint && check && test:unit -- --run && build) completed successfully with no failures; diff confined to markup/class/comment changes — no edits to `$lib/stores/domain/*` logic files (only removed a now-unused `healthStatus` import/derived cardBgClass in CombatantRow.svelte, a pure display change, not a mechanics change) |
| Note line renders read-only under card whenever set, collapsed AND expanded | PASS | CombatantRow.svelte:647-649 `{#if !open && combatant.note !== ''}` renders read-only `<p>` when collapsed; expanded case already covered by existing inline Textarea (CollapsibleContent, unchanged) which shows the note text |
| `npm run gate` stays green | PASS | Bash run of `npm run gate` completed through lint, check, test:unit, build with no errors (build output confirmed, no failure exit) |

## Scope check
In-scope only. All edited files are the declared Combat-screen components (CombatHeader, CombatantForm, CombatantRow, ConditionIconList, ConditionPicker, HealthBar, InitCell, NumberField, NumpadSheet, +page.svelte) plus the new JumpToTurnButton.svelte. No edits found to Combats-home/AppShell/Settings/About (unit D scope), no changes to `specs/design/*` prototype/token files, no changes to `messages/*.json` (JumpToTurnButton reuses pre-existing `active.jumpToTurn` / `a11y.jumpToTurn` keys — confirmed present in messages/en.json before this diff), no changes to `$lib/stores/domain/*` logic. `change.md` status bump (approved→verifying) is the expected lifecycle edit, not a scope violation.

## Other findings
None. The removal of `healthStatus`-driven `cardBgClass` in CombatantRow.svelte (bloodied/dead card background tint) is called out in the file's own updated header comment as an intentional "R4 restyle" removal (health-bar fill color now the sole HP-status signal) and is consistent with the change's stated scope (styling/DOM-visibility restyle) — flagged here for visibility since it changes a previously-relied-on visual cue, but it is not a game-mechanics change and is documented in-file.
