# Verification: 018-routing-fallback-not-found

| AC | Verdict | Evidence |
|----|---------|----------|
| vercel.json catch-all rewrite `/(.*)` -> `/index.html` | PASS | combat-planner-018/vercel.json:4-9 adds `"rewrites": [{"source": "/(.*)", "destination": "/index.html"}]`; `framework: null` unchanged, so Vercel's default filesystem-check-then-rewrite order applies — real static assets under `build/` still resolve directly. |
| Deep-link/hard-reload to `/combats/<id>` resolves to app-shell | PASS | Same rewrite is unconditional (`source: "/(.*)"`), covering param and unmatched paths alike — no exclusion pattern narrows it. |
| Root client catch-all route renders styled not-found w/ visible + aria-labelled Back-to-Combats control | PASS | combat-planner-018/src/routes/[...catchall]/+page.svelte:14-17 — `<p>{m['route.notFound.title']()}</p>` plus `<Button aria-label={m['route.notFound.back']()} onclick={() => goto('/combats')}>{m['route.notFound.back']()}</Button>` — visible text label and `aria-label` both present, uses app `Button` component/tokens. |
| Catch-all does not shadow real routes; `/combats/<bad-id>` still renders CLS-5 screen | PASS | combat-planner-018/src/routes/combats/[id]/+page.svelte:142-148 unchanged (`combat.notFound.title`/`combat.notFound.back` branch, untouched by diff — confirmed via `git diff` showing no changes to this file). SvelteKit route specificity gives concrete segments (`combats`, `settings`, `about`, `combats/[id]`) priority over the root `[...catchall]`; build output lists distinct entries for `combats/_page`, `combats/_id_/_page`, `settings/_page`, `about/_page`, and `_...catchall_/_page` as separate route nodes, confirming no shadowing. |
| Genuine thrown errors still surface PLT-10 `+error.svelte` boundary | PASS | No changes to `+error.svelte` or any load function that throws (`git status --short` shows only messages/*.json, vercel.json, and the new catchall route touched); catch-all component contains no error-throwing logic, only a static not-found render. |
| i18n: new key(s) distinct from `combat.notFound.*`, added to all 6 locales; paraglide regenerated not hand-edited | PASS | `route.notFound.title` / `route.notFound.back` added to messages/{en,de,es,fr,ja,ru}.json (diff shown in all 6 files); generated files `src/lib/paraglide/messages/route_notfound_title1.js` and `route_notfound_back1.js` exist on disk and are gitignored (`.gitignore:33` excludes `src/lib/paraglide`), consistent with "regenerated, never hand-edited" — not tracked in git, so no hand-edit diff to inspect, but presence + naming confirms `npm run build`'s Paraglide step regenerated them from the new keys. |
| `npm run gate` passes (lint + check + test:unit --run + build) | PASS | Ran `CHOKIDAR_USEPOLLING=true npm run gate` in the worktree — chain (`lint && check && test:unit -- --run && build`) completed through to a successful `build` step (adapter-static "Wrote site to build", PWA precache generated), confirming all prior `&&`-chained steps exited 0. Also ran the colocated test file directly (`vitest run src/routes`): 1 file / 1 test passed — `catchall-page.svelte.spec.ts` asserts `getByText('Page not found')` and `getByRole('button', { name: 'Back to Combats' })`. |

## Scope check
In-scope only. `git status --short` in the worktree shows exactly: `messages/{de,en,es,fr,ja,ru}.json` (modified), `vercel.json` (modified), `src/routes/[...catchall]/` (new, containing `+page.svelte` + `catchall-page.svelte.spec.ts`), plus the untracked `specs/changes/` (this change unit's own files, expected). No edits to `src/routes/combats/[id]/+page.svelte` (CLS-5 untouched, verified by absence from `git status`), no `+error.svelte` changes (PLT-10 untouched), no adapter change in `svelte.config.js`/`package.json` (not in diff), no redirect-to-home logic added (catch-all uses `goto('/combats')` only on explicit button click, matching the CLS-5 pattern, not an automatic redirect). All matches the declared file-ownership list in change.md exactly.

## Other findings
None. The `[...catchall]` route mirrors CLS-5's not-found markup/pattern (same flex-column-centered layout, same Button + goto('/combats') idiom) using a distinct i18n key pair, which is consistent with the change.md's "DRY extraction permitted but not required" allowance — no shared component was extracted, and CLS-5's own file was not touched, so no regression risk there. Pre-existing 2 lint warnings in specs/design/prototype.html noted in gate output are unrelated and non-blocking, consistent with the env notes.
