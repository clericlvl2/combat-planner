<script lang="ts">
	import { m } from '$lib/i18n';
	// TODO M-phase: Combats home (UX §3 / Component Inventory §5) —
	// CombatList → CombatRow, FAB(create), ImportControl, EmptyState, CombatFormDialog.
</script>

<h1>{m['combats.title']()}</h1>
