# Doc Verification: 024-compact-card-dims

| AC | Verdict | Evidence |
|----|---------|----------|
| platform.md: PLT-2 amended with combatant-card 40px exception; other ≥44px claims intact | PASS | specs/capabilities/platform.md diff — Touch-target paragraph adds "one deliberate card-scoped exception" (chevron/⋮/HP row 40px below `lg`) and cites WCAG 2.2 AA 2.5.8; AC bullet changed to "All interactive targets measure at least 44px... except the combatant card's chevron, ⋮ menu trigger, and unified HP tap row, which measure 40px below lg"; rest of file (other AC bullets, other breakpoint text) untouched |
| combatants.md: CBT-2 card-control touch-target prose/AC updated to 40px card exception | PASS | specs/capabilities/combatants.md diff — prose changed from "card's ⋮ overflow trigger is a ≥44px touch target" to "card's chevron, ⋮ overflow trigger, and unified HP tap row each measure 40px on the touch axis — a deliberate card-scoped exception..."; matching AC bullet updated identically; cross-links to [[hp]] HP-4 and [[platform]] PLT-2 added |
| component-inventory.md: --card-pad 10px / --hp-size 16px prose | PASS | diff: "`--card-pad` is `10px` and `--hp-size` is `16px` (unit 024... was `12px`/`18px`...)" replaces the stale 12px/18px "documented exception" paragraph |
| component-inventory.md: 22px chips | PASS | diff adds "card chips... render 22px tall in `CombatantRow`/`InitCell`/`ConditionIconList`, vs. the prototypes' `.chip` literal staying 24px"; also updates the Row-3 init-pill prose ("sized to a 22px chip dimension... down from 24px") and the condition-chips bullet ("22px tall in the shipped app... down from 24px") |
| component-inventory.md: 28px desktop icon buttons, desktop HP-row ≤32px | PASS | diff: "desktop (`lg:`) icon buttons (chevron, `⋮`) render 28px and the desktop HP tap row renders ≤32px in `CombatantRow`, vs. the prototypes' `.icon-btn` literal staying 32px" |
| component-inventory.md: deliberate-prototype-divergence paragraph (app diverges from prototypes' 24px/32px literals AND mobile rows 40px app-only) | PASS | new "**Deliberate prototype divergence (unit 024).**" paragraph covers both the desktop 22px/28px/32px non-tokenized divergence and the mobile 44px→40px divergence ("app and prototype now diverge on mobile card dims too, not just desktop"), with a card-scoped-exception cross-ref to PLT-2 |
| component-inventory.md: token-source-of-truth section consistent | PASS | opening paragraph (tokens.css single-source, `@import`ed by prototypes + layout.css) left intact; only the stale "documented exception" sub-clause was replaced, no contradiction introduced |
| CHANGELOG.md: exactly one 024-compact-card-dims row, describing shipped state (incl. 44→40px mobile rows, CBT-2+PLT-2 amended) | PASS | single new row added (2026-07-17) covering tokens.css deltas, desktop 28px/≤32px/22px shrinks, mobile 44→40px ("a deliberate card-scoped exception to PLT-2's ≥44px floor... amending both CBT-2 and PLT-2"), and the prototype-divergence note; `grep -c "024-compact-card-dims" specs/CHANGELOG.md` = 1 (row line only, filename/id not repeated elsewhere) |
| CHANGELOG.md: 023-condition-picker-pill-restyle row still exists | PASS | row present immediately above the new 024 row, untouched by this diff (`ConditionPicker.svelte`'s selected toggle-pill soft-fill restyle, CND-3) |

## Scope check
Doc diff touches exactly `specs/capabilities/platform.md`, `specs/capabilities/combatants.md`, `specs/reference/component-inventory.md`, `specs/CHANGELOG.md` — matches change.md's declared reference/capability-doc surface (CBT-2, PLT-2 amendments plus the tokens.css/component-inventory "Reference/design-source files updated alongside" note). No edits found outside these four files (`git diff HEAD -- specs/capabilities/ specs/reference/ specs/CHANGELOG.md` shows no other paths). No edits to `specs/design/tokens.css`, `card-prototype.html`, or `prototype.html` in this doc pass (those were code-pass/no-diff items already verified in verification.md). Scope respected.

## Other findings
None. All four amended docs are mutually consistent on the 40px mobile / 28px desktop / ≤32px HP-row / 22px chip dims, and none contradicts the shipped reality confirmed in verification.md (8/8 PASS). No stale 44px claims remain for the combatant card specifically; the app-wide 44px floor is correctly preserved as the default everywhere else (platform.md's PLT-2 AC list, combatants.md CBT-2 general prose) with only the card's three rows carved out.
