# Verification: 025-condition-picker-colorized

| AC | Verdict | Evidence |
|----|---------|----------|
| Pill styled via inline `--tc` sourced from per-condition accent map | PASS | `src/lib/components/app/ConditionPicker.svelte:52` — `style="--tc: {conditionAccent[c]}"`; `src/lib/components/app/labels.ts:42-56` — `conditionAccent: Record<Condition, string>` maps each condition to its card `--combat-*` var (matches `conditionColor` hues 1:1, e.g. `weakened: 'var(--combat-red)'` ↔ `weakened: '...text-combat-red'`) |
| Unselected pill: tinted fill/border/text via color-mix, `!rounded-[12px]`, no dot/icon | PASS | `ConditionPicker.svelte:53` class string: `!rounded-[12px] ... bg-[color-mix(in_srgb,var(--tc)_9%,var(--popover))] border-[color-mix(in_srgb,var(--tc)_28%,var(--border))] text-[var(--tc)]`; item body is `{conditionLabel[c]()}` only, no icon/dot markup |
| Selected pill: deeper fill, solid `border-[var(--tc)]`, `ring-1 ring-[var(--tc)]`, mixed text — utilities present on BOTH `data-[state=on]:` and `aria-pressed:` | PASS | `ConditionPicker.svelte:53` has both variant groups with identical selected utilities: `data-[state=on]:bg-[color-mix(in_srgb,var(--tc)_18%,var(--popover))] data-[state=on]:border-[var(--tc)] data-[state=on]:ring-1 data-[state=on]:ring-[var(--tc)] data-[state=on]:text-[color-mix(in_srgb,var(--tc)_55%,var(--foreground))]` and `aria-pressed:bg-[color-mix(in_srgb,var(--tc)_18%,var(--popover))] aria-pressed:border-[var(--tc)] aria-pressed:ring-1 aria-pressed:ring-[var(--tc)] aria-pressed:text-[color-mix(in_srgb,var(--tc)_55%,var(--foreground))]` — both prefixes override `bg-muted`/border/text so cascade can't leave either variant unstyled |
| No reflow: font-weight constant rest/hover/selected | PASS | `ConditionPicker.svelte:53` sets `font-medium` once in the base (unprefixed) class list; no `hover:font-*` or `data-[state=on]:font-*`/`aria-pressed:font-*` utility present anywhere in the string |
| Picker lists 12 conditions alphabetically by localized label, reactive to language change | PASS | `ConditionPicker.svelte:39` — `const sorted = $derived(sortConditions([...CONDITIONS]));`, iterated at line 51 (`{#each sorted as c (c)}`); `labels.ts:96-98` — `sortConditions` calls `conditionLabel[a]()` (a Paraglide `m[...]` message fn) inside `.sort`, so the `$derived` re-evaluates whenever the reactive locale changes and the message fns return new strings — same reactivity pattern already used by every other `conditionLabel[c]()` call site in the app |
| `ConditionIconList.svelte` card chips ordered alphabetically, chip visual style unchanged | PASS | `ConditionIconList.svelte:22` — `const sorted = $derived(sortConditions(conditions));`, iterated at the `{#each sorted as c (c)}` loop; `Badge` class string (`h-[22px] gap-1.5 px-2.5 text-sm font-normal`, `conditionColor[c]`) is byte-identical to the pre-change version — only the array being iterated changed (`conditions` → `sorted`) |
| Gate stays green (`npm run gate`) | PASS | `CHOKIDAR_USEPOLLING=true npm run gate` — exit 0; lint reported `0 ERRORS 0 WARNINGS`; build completed (`✔ done`) |

## Scope check
In-scope only. `git status --porcelain` shows edits confined to the three declared owned files (`src/lib/components/app/labels.ts`, `ConditionPicker.svelte`, `ConditionIconList.svelte`) plus the change-unit's own spec artifacts (`change.md`, `tasks.md`). No edits to `specs/design/*.html`, `tokens.css`, `messages/*.json`, or any other component. `CND-1`/`CND-2` apply/remove logic, the Dialog/Drawer trigger structure, and the "+K" overflow behavior are untouched in the diff.

## Other findings
None. The new `conditionAccent` map's hue-per-condition assignments line up exactly with the pre-existing `conditionColor` map (same `--combat-*` var per condition), so the picker's accent and the card chip's color for a given condition stay visually consistent — no contradiction introduced.
