# Verification: 012-numberfield-input-fixes

| AC | Verdict | Evidence |
|----|---------|----------|
| Value input is `type="text"` not `type="number"` | PASS | src/lib/components/app/NumberField.svelte:108 (`type="text"`), no `type="number"` remains |
| `inputmode`/keyboard hint exposes minus key | PASS | src/lib/components/app/NumberField.svelte:109 (`inputmode="text"`); leading `-` accepted via `sanitize()` (:47-51) and committed as negative via `commit()` (:72-83) |
| Typed entry sanitized via regex on `oninput` to allowed set | PASS | src/lib/components/app/NumberField.svelte:47-51 (`sanitize`), 55-61 (`onTypedInput`, wired at :121); intermediate states (`-`, `1e`, `1.`) reduce to digits+sign, never blank |
| Commit-time `clamp(n,min,max)` preserved, `errors.clamp` hint unchanged | PASS | src/lib/components/app/NumberField.svelte:79-83 (`clamp` call unchanged), :133-135 hint markup unchanged (only diff context, no changed lines there); confirmed by passing `CombatantForm.svelte.spec.ts` clamp test (updated only to expect string `'1'` instead of numeric `1`, same assertion of clamped value + visible hint) |
| Negative bounded entry: initiativeBonus −99..99, initiative −9..99, clamp on commit | PASS | Domain ranges untracked/unchanged (RANGES not touched per diff); NumberField.svelte.spec.ts:11-38 exercises min=-9,max=99 paste-then-clamp path; commit()/clamp() logic (:79-83) is range-agnostic and applies to any min/max passed by call sites, which are unchanged |
| B-013: digit-cap retained for typed input; paste/programmatic path bypasses cap, unit test drives it and asserts clamp fires | PASS | digit-cap retained: NumberField.svelte:53-61 (`onTypedInput` still slices to `digitCap`); bypass path: :63-70 (`onPaste`, sanitizes but no digitCap slicing), wired at :122; test: src/lib/components/app/NumberField.svelte.spec.ts:11-38 pastes `'12345'` (over cap of 2 for max=99), asserts `input.value === '12345'` unrestricted, then fires `change` and asserts value clamps to `'99'` and `errors.clamp` hint visible |
| Stepper buttons carry aria-labels from 2 new i18n keys, present in all 6 locales, Paraglide regenerated | PASS | NumberField.svelte:101 (`aria-label={m['a11y.numField.decrease']()}`), :127 (`increase`); keys added in messages/de.json, en.json, es.json, fr.json, ja.json, ru.json (all diffs show `a11y.numField.decrease`/`a11y.numField.increase` added); Paraglide regenerated — src/lib/paraglide/messages/a11y_numfield_decrease1.js and a11y_numfield_increase1.js exist on disk (gitignored, confirmed via `ls`) |
| `getByLabelText(fieldLabel)` still resolves to value input alone, existing tests stay green | PASS | NumberField.svelte.spec.ts:40-53 (new test explicitly asserts `el.tagName === 'INPUT'` for `getByLabelText`); CombatantForm.svelte.spec.ts diff shows only value-format assertions changed (`null`→`''`, `1`→`'1'`), label-resolution assertions untouched; `npm run gate` unit-test step: 133 tests passed, 30 files passed |
| `.numfield` shell visual unchanged (−/value/+, 44px targets), `errors.clamp` hint placement unchanged | PASS | Outer wrapper div (NumberField.svelte:95-97), button classes (`stepBtnClass` :42-43, `min-h-11 w-11`), and hint paragraph (:133-135) all unchanged in the diff — only `Input` element's attributes (type/inputmode/role/aria-value*) and handlers changed, and the two buttons gained only `aria-label` props |
| `npm run gate` passes (lint, check, unit tests, build) | PASS | Ran `npm run gate`: Biome check 0 errors/warnings; svelte-check 0 problems; 30 test files / 133 tests passed; vite build + PWA build completed ("✓ built", "Wrote site to build") |

## Scope check

In-scope only. Changed files: `messages/{de,en,es,fr,ja,ru}.json` (new i18n keys), `specs/backlog.md`
(B-013 status update, expected as part of closing the backlog item this unit promotes),
`src/lib/components/app/NumberField.svelte` (the declared single component), plus test files
`src/lib/components/app/CombatantForm.svelte.spec.ts` (updated only for the type="text" value-format
change) and new `src/lib/components/app/NumberField.svelte.spec.ts`.

Verified untouched per Out of scope: `NumpadSheet.svelte`, `src/lib/components/ui/input/input.svelte`,
`InitCell.svelte`, `CombatHeader.svelte`, `CombatantForm.svelte` (`git diff --stat` on all five shows
no output — no changes). `src/lib/paraglide/*` not hand-edited — it is gitignored/generated output,
regenerated by the build (confirmed the two new message files exist on disk, not in git diff).

## Other findings

None. The `onPaste` handler's manual `sanitize(before + pasted + after)` reconstruction correctly
accounts for `selectionStart`/`selectionEnd` for mid-string pastes. One minor style note (not an AC
regression): `onPaste` line 69 is a long single line (~140 chars) — cosmetic only, Biome/lint gate
passed clean so it's within configured line-length limits.
