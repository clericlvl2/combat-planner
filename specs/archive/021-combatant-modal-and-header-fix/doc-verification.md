# Doc Verification: 021-combatant-modal-and-header-fix

| AC | Verdict | Evidence |
|----|---------|----------|
| CBT-3 prose/AC rewritten to describe capped/scrolling dialog, stacked Name/Type/Note, 2-across NumberField pairs, restyled footer | PASS | specs/capabilities/combatants.md diff — new paragraph + 2 new/rewritten AC bullets match code-pass verified src/lib/components/app/CombatantForm.svelte:109-111 (max-h-[calc(100dvh-2rem)] flex flex-col), :129 (overflow-y-auto wrapper), :131-227 (stacked fields), :170-245 (grid grid-cols-2 pairs), :248-257 (footer variant=outline, h-11 flex-1) |
| CBT-4 prose/AC amended for field order (Init Bonus last stat field) and conditional Initiative field (Setup-add: Init Bonus only; edit/Active-add: Initiative shown) | PASS | specs/capabilities/combatants.md new paragraph + 2 new AC bullets match CombatantForm.svelte:229-245 (`{#if mode === 'edit' \|\| (mode === 'add' && combatActive)}` gating `cf-init` next to unconditional `cf-initbonus`) |
| PLT-2 amended: combat header chrome caps at 1024px (`--content-max-wide`/`.content-container-wide`), sub-bar/cards/other body content stay at 768 | PASS | specs/capabilities/platform.md new sentence in PLT-2 body + new AC bullet match specs/design/tokens.css:41 (`--content-max-wide: 1024px`), src/routes/layout.css:64-67/151-160 (`@theme inline` exposure + `@utility content-container-wide`), src/lib/components/app/CombatHeader.svelte:90 (chrome div uses `content-container-wide`) vs. line 192 (sub-bar keeps `content-container`) |
| `specs/reference/component-inventory.md` re-synced — "Desktop content container" section gains the unit-021 `--content-max-wide`/`.content-container-wide` paragraph; `CombatantForm` hierarchy entry gains the unit-021 dialog-layout note | PASS | diff shows both additions; text accurately states "consumed only by `CombatHeader.svelte`'s own chrome wrapper" (matches CombatHeader.svelte:90, no other consumer found) and accurately describes CombatantForm's stacked/2-across/capped-dialog/footer changes matching the same code lines cited above |
| `specs/CHANGELOG.md` — row exists for unit 021 | PASS | specs/CHANGELOG.md — `\| 021-combatant-modal-and-header-fix \| 2026-07-16 \| ...` row present after the 020 row, summarizing the CBT-3/CBT-4/PLT-2 changes accurately |

## Scope check
In-scope only. Diff touches exactly `specs/capabilities/combatants.md`, `specs/capabilities/platform.md`, `specs/reference/component-inventory.md`, `specs/CHANGELOG.md` — the four files this doc-pass was scoped to. No edits found to any capability file outside CBT-3/CBT-4/PLT-2 (spot-checked CBT-2, CBT-5..8, PLT-1/3/4/5..12 sections — untouched, diff shows no hunks there), and no edits to component-inventory sections outside the "Token source of truth" paragraph and the `CombatantForm` hierarchy bullet.

## Other findings
None. Prior stale claim in component-inventory.md ("`CombatHeader.svelte` all apply [content-container]") was corrected in this pass to "the Round/Escalation sub-bar in `CombatHeader.svelte`" — this is a necessary, in-scope correction given the new 1024px chrome cap, not scope creep. No contradictions with adjacent, unamended AC bullets (e.g. PLT-3 header-control-ordering language, CBT-2 card fields) were introduced.
