# Doc Verification: 014-unified-design-tokens

| Doc | Verdict | Evidence |
|-----|---------|----------|
| `specs/capabilities/settings.md` (SET-2) | PASS | Diff adds "Applied via a `[data-theme=\"light\"\|\"dark\"]` attribute on the document root (not a `.dark` class)... no new theme is exposed in the UI yet." Confirmed against `src/lib/theme.ts:19` (`document.documentElement.dataset.theme = isDark ? 'dark' : 'light'`) — mechanism-only, correctly describes the shipped attribute, correctly notes no new theme UI. No overstatement. |
| `specs/capabilities/platform.md` (PLT-5) | PASS | New paragraph under PLT-5 AC: "Known exception, tracked not silently accepted: a token-relocation-only pass (unit 014) recomputed AA from actual hex and surfaced pre-existing light-theme gaps... `--text-faint`, `--health-wounded`/`--health-bloodied`, blue/orange/green/neutral/teal combat swatches... No token value changed to fix these; deferred to backlog **B-028**." Matches `specs/backlog.md` B-028 row verbatim in gap list. Explicitly does NOT claim gaps are fixed — states "No token value changed to fix these." Correctly preserves the pre-existing "Both themes meet AA" bullet unmodified (adds the exception as a new bullet rather than editing/weakening the original claim, consistent with change.md's "amend (preserve)" framing). |
| `specs/reference/component-inventory.md` | PASS | New "Token source of truth" paragraph names `specs/design/tokens.css` as holding all raw tokens (colors, `--space-*`, `--font-*`, radius scale, component dims) and lists the `@import` in `prototype.html`/`card-prototype.html`/`layout.css`. Confirmed: `specs/design/tokens.css:36` `--card-pad: 12px`, `:40` `--hp-size: 18px`, doc states these are the documented 12px/18px exception vs. prototype's prior 14px/19px — matches change.md's exception clause exactly. Card-shape section addendum correctly states `CombatantRow` references `var(--card-pad)` etc.; confirmed in `src/lib/components/app/CombatantRow.svelte:109,121,123,176,180` (`p-[var(--card-pad)]`, `gap-[var(--card-gap)]`, `text-[length:var(--hp-size)]`, `w-[var(--badge-width)]`, `border-[length:var(--card-border)]`). |
| `specs/CHANGELOG.md` (014 row) | PASS | New row accurately summarizes: tokens.css as SSOT, three `@import` sites, `.dark`-class → `[data-theme]` migration (`theme.ts`, `@custom-variant dark` remap — confirmed in `src/lib/theme.ts` and expected `layout.css` diff), the 12px/18px documented exception, and the WCAG-AA gaps deferred to B-028 (not fixed). Promotes B-027 — matches `specs/backlog.md` B-027 row (status `in-unit`, pointing at this change dir). No claim exceeds what's in the code-pass verification.md (all-PASS) or the ground-truth files read directly. |

## Scope check
In-scope only — doc-syncer touched exactly the four declared files (`settings.md`, `platform.md`, `component-inventory.md`, `CHANGELOG.md`) plus `specs/backlog.md` (expected bookkeeping: B-027 promoted in-unit, B-028 idea row added, both cross-referenced correctly by the four docs above). No edits found outside this set in `git status`/`git diff` for the doc layer.

## Other findings
None. Cross-checked every doc claim against actual shipped code (`src/lib/theme.ts`, `specs/design/tokens.css`, `src/lib/components/app/CombatantRow.svelte`) rather than trusting the code-pass verification.md's paraphrase, and all matched byte-for-byte where checkable (hex hold in tokens.css, `dataset.theme` assignment, `var(--card-pad)` etc. call sites). The PLT-5 addition is careful to add a new "known exception" bullet rather than silently weakening the pre-existing "both themes meet AA" bullet — this is the correct non-destructive amend pattern and avoids contradicting the older AC text.
