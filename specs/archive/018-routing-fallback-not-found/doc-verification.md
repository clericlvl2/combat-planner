# Doc-verification: 018-routing-fallback-not-found

| Doc edit | Verdict | Evidence |
|----|---------|----------|
| PLT-9 amended (Vercel SPA fallback AC) | PASS | `vercel.json:5-10` in worktree adds `"rewrites": [{"source": "/(.*)", "destination": "/index.html"}]` with `framework: null` unchanged — matches the amended prose ("catch-all rewrite applied after the filesystem check … real static assets still resolve directly," Vercel's default rewrite-after-filesystem-check behavior). New AC bullet text ("`vercel.json` contains a catch-all rewrite sending unmatched requests to `/index.html`… real static assets under `build/` still resolve directly") accurately describes the shipped config; no overclaim (doesn't claim server-side logic, doesn't claim adapter change). |
| New PLT-12 (unknown-path recovery screen) | PASS | `src/routes/[...catchall]/+page.svelte` renders a static `<p>` title + `<Button>` with both visible text and `aria-label={m['route.notFound.back']()}` calling `goto('/combats')` on click — matches spec's "styled not-found screen … visible, aria-label-labelled Back-to-Combats control … navigates to `/combats` when activated." Spec correctly says "no error is thrown" / "never intercepts a genuinely thrown error" — component contains no throw, no error boundary interaction, confirmed by unchanged `src/routes/+error.svelte` (absent from diff). Spec correctly avoids describing a redirect — code does an explicit on-click `goto`, not an automatic redirect on render. Route-shadowing claim (`/combats`, `/combats/[id]`, `/settings`, `/about`, and CLS-5's bad-id screen unaffected) verified by diff --stat showing zero touches to `src/routes/combats/[id]/+page.svelte` or any other existing route file — only new `src/routes/[...catchall]/` added. |
| New `route.notFound.title` / `route.notFound.back` i18n rows | PASS | `messages/en.json` contains `"route.notFound.title": "Page not found"`, `"route.notFound.back": "Back to Combats"`; same two keys present (non-empty) in `de/es/fr/ja/ru.json` per diff --stat (2 insertions each, all 6 locale files touched). i18n-catalog row correctly distinguishes `route.notFound.title` from `combat.notFound.title` (both exist side-by-side in en.json, different values would diverge but currently both read "Page not found"/"Combat not found" resp. and "Back to Combats" resp. — no collision, catalog note "distinct from `combat.notFound.title`" is accurate as a key-namespace claim, not a wording claim). New `route.*` line added to the key-family index. |
| New PLT-12 acceptance-matrix row | PASS | Row points at `catchall-page.svelte.spec.ts`, layer `C` (component). File exists at `src/routes/[...catchall]/catchall-page.svelte.spec.ts`, uses `vitest-browser-svelte` `render`, asserts `getByText('Page not found')` and `getByRole('button', { name: 'Back to Combats' })` — a component-layer test, layer tag correct. Description "no-route-match renders not-found title + labelled Back-to-Combats button" matches the two assertions in the spec file. |
| CHANGELOG.md new row for 018 | PASS | Row present, dated 2026-07-15, summarizes vercel.json rewrite + new catchall route + PLT-12 + i18n keys + "CLS-5 and PLT-10 unchanged" — consistent with the diffs inspected above; format matches preceding rows in the table. |

## Cross-check: unchanged capabilities
- CLS-5 (`src/routes/combats/[id]/+page.svelte`): confirmed absent from worktree diff — not touched.
- PLT-10 (`src/routes/+error.svelte`): confirmed absent from worktree diff — not touched.
- Both spec edits (PLT-9, PLT-12) explicitly state these are unchanged; matches code.

## Scope check
Doc diff touches exactly `specs/capabilities/platform.md`, `specs/reference/i18n-catalog.md`, `specs/reference/acceptance-matrix.md`, `specs/CHANGELOG.md` — no other spec files edited, no code files touched by the doc-syncer pass (all code changes belong to the prior implementer commit per `git diff --stat main`).

## Other findings
None — no overclaim, no contradiction of unrelated capability ACs found. PLT-12 prose correctly avoids describing a redirect (matches on-click `goto`, not render-time redirect) and correctly frames the catch-all as never intercepting thrown errors.
