# Verification: 014-unified-design-tokens

| AC | Verdict | Evidence |
|----|---------|----------|
| tokens.css exists, holds every raw token (8 swatches, 3 type colors, 4 health bands, neutral chrome, --space-*, --font-*, radius scale, component dims) verbatim, with the documented --card-pad/--hp-size 12px/18px exception | PASS | specs/design/tokens.css:9-127 has all listed tokens; --card-pad:12px (L36) and --hp-size:18px (L40) annotated as the change.md-documented exception (L35, L39); all color hexes match the removed layout.css/.dark block byte-for-byte (diff comparison) |
| prototype.html, card-prototype.html, layout.css each @import tokens.css; no raw literal for a shared token remains inline | PASS | prototype.html:26 `@import "tokens.css"`; card-prototype.html:15 `@import "tokens.css"`; layout.css:3 `@import "../../specs/design/tokens.css"`; grep for stray hex/space/font/radius/card-* literals in all three found only `var(--...)` consumption sites, no re-declared literals (removed blocks confirmed via `git diff`) |
| Themes keyed by [data-theme="light"]/[data-theme="dark"], :root theme-independent; no .theme-light/.theme-dark classes, no .dark-class palette block | PASS | tokens.css:47,88 `[data-theme="dark"]`/`[data-theme="light"]`; card-prototype.html diff removes `.theme-dark`/`.theme-light` blocks and rewrites markup to `data-theme="dark"/"light"` attributes (git diff shows classes replaced, e.g. `.theme-cell__surface theme-dark` → `.theme-cell__surface" data-theme="dark"`); layout.css diff removes the `.dark { ... }` block entirely; grep for `theme-dark\|theme-light\|\.dark ` across all three files returns nothing |
| @custom-variant dark remapped to [data-theme="dark"]; existing dark: utilities still resolve | PASS | layout.css:5 `@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *));`; NumpadSheet.svelte and ConfirmDialog.svelte `dark:` utility classes untouched (unmodified in diff) and depend only on the variant definition, which now maps correctly |
| theme.ts sets document.documentElement.dataset.theme (no classList.toggle('dark')); system resolves then sets attribute; theme-color meta still updates | PASS | src/lib/theme.ts:19 `document.documentElement.dataset.theme = isDark ? 'dark' : 'light'`; :20 `setThemeColorMeta(isDark)` still called; `resolve()` (L32) still handles `system` via matchMedia before `applyIsDark` |
| Spacing wired through Tailwind --spacing-* so p-4/gap-2 resolve to --space-*; shipping card references var(--card-pad)/var(--hp-size)/etc rather than literal px | PASS | layout.css:62 `--spacing: var(--space-1)` inside `@theme inline`; CombatantRow.svelte diff replaces `p-3`→`p-[var(--card-pad)]`, `gap-2`→`gap-[var(--card-gap)]`, `text-lg`→`text-[length:var(--hp-size)]`, `size-4`→`h-4 w-[var(--badge-width)]`, adds `border-[length:var(--card-border)]` |
| --radius-md/--radius-xl reconciled — prototype-backed, no app-invented token absent from prototype | PASS | tokens.css:11-15 defines --radius, --radius-sm, --radius-lg, --radius-md (calc), --radius-xl (18px) all in one place; layout.css `@theme inline` (L54-57) now references `var(--radius-md)`/`var(--radius-xl)` instead of re-declaring literals — single source, no orphan token |
| All 8 swatches, 3 type colors, 4 health bands resolve to same computed hex in both themes as before (no visual regression) | PASS | Byte-for-byte hex comparison of tokens.css `[data-theme="dark"]`/`[data-theme="light"]` blocks against the removed `:root`/`.dark` blocks in layout.css's prior version (via `git diff`) shows identical hex values for every --type-*, --health-*, --combat-* entry |
| Contrast unchanged vs shipped pre-relocation values; pre-existing light-theme AA gaps documented + deferred to B-028, not fixed here | PASS | prototype.html contrast-note table (L2298-2354) unchanged in values; specs/backlog.md diff adds B-028 row explicitly naming `--text-faint`, `--health-wounded`/`--health-bloodied`, and 5 combat swatches (blue/orange/green/neutral/teal) as light-theme AA gaps deferred, referencing "surfaced by unit 014"; change.md itself documents the same deferral in its AC and out-of-scope list; no token hex value was altered in this diff (aside from the two authorized dimension exceptions, which are not color/contrast values) |
| npm run gate passes (lint + check + test:unit + build) | PASS | ran `npm run gate` — completed with `0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`, Vite build succeeded, exit code 0 |

## Scope check
In-scope only. Touched files match the declared set: specs/design/tokens.css (new), src/routes/layout.css, src/lib/theme.ts, specs/design/prototype.html, specs/design/card-prototype.html, src/lib/components/app/CombatantRow.svelte. `src/lib/db/types.ts` was listed as a context file in the dispatch but has zero diff (`git diff` empty) — correctly untouched, `Theme` type already existed pre-change. `specs/backlog.md` was also touched (adds B-027 in-unit row + B-028 idea row) — this is expected bookkeeping for a change unit that promotes a backlog item and surfaces a new one, not scope creep into app/product code.

## Other findings
None. `dark:` utility call sites in NumpadSheet.svelte / ConfirmDialog.svelte were correctly left unmigrated per the out-of-scope list, and still function because only the `@custom-variant dark` selector definition changed, not the call sites themselves. No new theme was added to the UI. No token value (color hex) changed outside the two authorized dimension exceptions.
