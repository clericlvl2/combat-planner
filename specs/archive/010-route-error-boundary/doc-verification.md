# Doc verification: 010-route-error-boundary

| Item | Verdict | Evidence |
|------|---------|----------|
| PLT-10 new section — content matches shipped `+error.svelte` | PASS | `specs/capabilities/platform.md` new PLT-10 section (diff) claims: route-level `+error.svelte`, app tokens, chrome preserved, `role="alert"`/`aria-live`, recovery button w/ visible label + `aria-label`. All confirmed in `src/routes/+error.svelte`: `role="alert" aria-live="assertive"` div wrapping `EmptyState`; both `Button`s have `aria-label={m['appError.reload']()}` / `m['appError.goToCombats']()`; header comment confirms it renders inside `+layout.svelte`'s AppShell (chrome intact). Mechanism note ("reading page.error/page.status from $app/state") matches `import { page } from '$app/state'` and `data-error-status={page.status}` usage. |
| PLT-10 AC — thrown `/` load or `store.hydrate()`/Dexie error surfaces boundary | PASS | `specs/capabilities/platform.md` PLT-10 AC bullet 2 matches `src/routes/+page.ts` diff comment: "a genuine `store.hydrate()`/Dexie failure is left to propagate so the `+error.svelte` boundary can surface it (CLS-7 / PLT-10)" — code and doc cross-reference each other correctly. |
| PLT-11 new section — Back-button overlay-dismiss description matches shipped behavior | PASS | Doc: "sheets are not stacked today — at most one is open at a time" and "closing a sheet normally... consumes that history entry itself". This matches `verification.md`'s account of `closeTopOverlay()` priority order (numpad > edit > add) and the two `$effect`s pushing/consuming one history entry (`src/routes/combats/[id]/+page.svelte:74-101` per verification.md). Doc does not overstate — correctly scopes to "at most one" overlay, doesn't claim a stack. |
| CLS-5 amendment — not-found state + control back to `/combats` | PASS | Doc AC: "Navigating to `/combats/<nonexistent-id>` renders a not-found state including a visible, labelled control that navigates back to `/combats`." Matches verification.md evidence: `+page.svelte:159-165`, `{:else if !combat}` branch, `Button` with `aria-label={m['combat.notFound.back']()}` calling `goto('/combats')`. Doc correctly cross-references PLT-10 as the separate broader error boundary (not-found is not a thrown error) — accurate distinction, not a contradiction. |
| CLS-7 amendment — first-launch-no-combat fallback | PASS | Doc: "If first-launch seeding yields no combat... the app lands on `/combats`... normal first-launch behavior... unaffected." Matches `src/routes/+page.ts` diff exactly: `seededCombat` check redirects to combat page only if present, otherwise falls through to unconditional `redirect(307, '/combats')` — no throw. Normal-path (seeded combat exists) unchanged. |
| CHANGELOG.md row — well-formed, matches table format | PASS | `specs/CHANGELOG.md`: new row `| 010-route-error-boundary | 2026-07-11 | ... |` — matches existing 3-column `| unit | date | summary |` format used by all prior rows (008, 009). Date 2026-07-11 matches session's current date. Summary accurately lists all 4 changes (PLT-10, PLT-11, CLS-5, CLS-7) with correct file/spec pointers, ends with "Promotes B-026" consistent with change.md's `backlog: B-026` frontmatter. No overstatement — wording mirrors change.md's "What changes" table closely without copying verbatim. |

## Scope check
In-scope-only. Only `specs/capabilities/platform.md`, `specs/capabilities/combats-list.md`, and `specs/CHANGELOG.md` were touched by the doc-syncer, matching the dispatch scope. No edits found to unrelated capability files (hp.md, initiative.md, conditions.md, etc.) or to code.

## Other findings
- No contradiction found between the CLS-5 amendment and PLT-10's own scope note — both explicitly disambiguate "not-found is not a thrown error" vs. "PLT-10 owns thrown errors," consistent in both directions.
- `specs/capabilities/combats-list.md` CLS-7's added AC bullet ("without throwing an unhandled error") is a slightly weaker/safer phrasing than change.md's AC ("no unhandled error is thrown") — equivalent meaning, not a discrepancy.
- No regression or stale cross-reference spotted in the diffed sections.
