# Verification: 009-visual-fidelity-cure

| AC | Verdict | Evidence |
|----|---------|----------|
| Theme persists on reload from any route | PASS | `src/lib/theme.ts` (new, `applyTheme`) + `src/routes/+layout.svelte:16-20` calls it in a root `$effect` keyed on `store.settings.theme`; `src/routes/settings/+page.svelte` no longer toggles the class itself (removed effect, lines 49-68 of old file) |
| `theme-color` meta reflects active theme | PASS | `src/lib/theme.ts:13-16` (`setThemeColorMeta`) sets `#101317`/`#f4f5f7` on every `applyIsDark` call; `src/app.html:6` default comment notes it's runtime-updated |
| Tailwind `text-xs/sm/base/lg/xl` → design scale; no `text-[10px]`/`text-[11px]` in CombatantRow/ColorTagDot | PASS | `src/routes/layout.css:170-180` (`--text-xs: var(--font-xs)` etc.); `src/lib/components/app/ColorTagDot.svelte:45` `text-xs` (was `text-[11px]`); `src/lib/components/app/CombatantRow.svelte:184` uses `text-xs`/`text-lg` (was `text-[10px]`); regex confirmed no `text-[10px]`/`text-[11px]` left in `src/lib` or `src/routes` |
| shadcn overlays render `--shadow-md` not Tailwind stock | PASS | `src/routes/layout.css:172-180` aliases `--shadow-sm`/`--shadow-md` (Tailwind's tokens) onto the renamed `--shadow-design-sm`/`--shadow-design-md` (former literal `--shadow-sm`/`--shadow-md` values, moved to lines 32-33) |
| Combatant card = card-prototype recipe (12px radius, token border, 6px stripes w/ gap for PC, active `▶` dot, 24px init chip w/ ≥44px target, visible 8px health-bar track, offset temp-HP badge, bold def-stat values) | PASS | `src/lib/components/app/CombatantRow.svelte:107-112` (`rounded-[12px] border border-border`), `:115-119` (`gap-1` between stripe spans, `typeStripeCount.pc = 2` in unchanged `labels.ts:28`), `:124-126` (`▶` dot, `active` guard), `:179-186` (temp-HP badge repositioned to `-right-5`, outside the number), `:194-198` (bold `statClass` on AC/PD/MD); `src/lib/components/app/InitCell.svelte:71-85` (24px `h-6` chip nested inside a `min-h-11 min-w-11` ghost button); `src/lib/components/app/HealthBar.svelte` (pre-existing, untouched) — `h-2` (8px) track with `bg-muted` |
| Desktop (≥1024px) Active header shows Advance/Jump roundels; mobile FAB/pill hidden at that bp; Round/Escalation as label/value pairs | PASS | `src/lib/components/app/CombatHeader.svelte:119-141` (`hidden ... lg:inline-flex` roundels), `:188-224` (uppercase label span + tabular value span for round/escalation); `src/routes/combats/[id]/+page.svelte:133,152` (`lg:hidden` on Advance FAB); `src/lib/components/app/JumpToTurnButton.svelte:20` (`lg:hidden` on pill) |
| Setup Start FAB reads as primary | PASS | `src/routes/combats/[id]/+page.svelte` diff removes `variant="secondary"` from the Start FAB, leaving the `Button` default (primary-filled) variant |
| NumpadSheet commit buttons meet WCAG-AA in both themes; History is collapsible w/ chevron | PASS | `src/lib/components/app/NumpadSheet.svelte:122-152` (`variant="outline"` + `border-*/30` + `text-*`, no bg fill) — recomputed contrast independently from `src/routes/layout.css` token hexes: light `--destructive`(`#b91c1c`)/white ≈6.47:1, `--health-full`(`#0b815a`)/white ≈4.88:1, `--combat-blue`(`#0b7caf`)/white ≈4.65:1; dark `--destructive`(`#ef4444`)/`--surface`(`#171b21`) ≈4.59:1, `--health-full`(`#34d399`) ≈8.99:1, `--combat-blue`(`#38bdf8`) ≈8.07:1 — all ≥4.5:1 AA text threshold (Sheet uses `bg-popover` = `--surface`, confirmed in `src/lib/components/ui/sheet/sheet-content.svelte:38`); `:196-204` adds `Expand` chevron icon with `rotate-180` on open |
| Combats list: untitled placeholder, row radius = `--radius`, create aria-label "New combat" | PASS | `src/lib/components/app/CombatRow.svelte:37,49` (`displayTitle` derived fallback to `m['combats.untitled']()`); `:41` (`rounded-[var(--radius)]`, and `layout.css:17` `--radius: 10px`); `src/routes/combats/+page.svelte:62-63,81,94` (`aria-label={m['combats.create']()}`, and `messages/en.json` `combats.create` = "New combat") |
| `SearchField` filters combats list by title in real time, view-local `$derived`, shown only when list non-empty; new i18n key; docs updated | PASS | `src/lib/components/app/SearchField.svelte` (new); `src/routes/combats/+page.svelte:26-31` (`$derived` filter, not written to `store`), `:83-97` (`SearchField` only in the `{:else}` branch, i.e. non-empty); `messages/en.json` `combats.search.placeholder`; `specs/capabilities/combats-list.md` new `## CLS-9 — Search` section; `specs/reference/component-inventory.md` `SearchField` entry (replacing `SearchBar` placeholder); `specs/reference/i18n-catalog.md` new key row |
| `card-prototype.html` light `--health-*`/`--combat-*` hexes corrected to `prototype.html` values | PASS | `specs/design/card-prototype.html` diff: `--health-full`/`wounded`/`bloodied` and all `--combat-*` swatches now match `specs/design/prototype.html`'s light-theme values (e.g. `--health-full: #0b815a` in both) |
| No game-mechanics/data change; existing tests pass; `npm run gate` green | PASS | `git diff --stat` shows zero touched files under `src/lib/domain/**` or `src/lib/stores/**` (only view components, `theme.ts`, docs, i18n, agent/skill config); `npm run gate` run — lint/check/test/build all completed with `0 ERRORS 0 WARNINGS` and a successful build |

## Scope check

Out-of-scope edits found (not declared in change.md's "What changes" table or out-of-scope list):

- `.claude/skills/spec-orchestrate/SKILL.md` — added a "Hard boundary" section on agent delegation. Not process work for this visual-fidelity change; unrelated to any AC. Flag.
- `specs/backlog.md` — added B-020 through B-026 (7 new backlog items: task-spec scoping, header unification, jump-to-turn removal, animations, first-launch UX, credentials, routing edge cases). None of these are declared in change.md; they read as opportunistic backlog capture from the same session, not required by any AC. Flag as out-of-scope (low risk — additive backlog rows, no behavior change — but still outside declared file ownership).
- `.claude/agents/spec-planner.md` and `CLAUDE.md` design-truth-rule additions ARE declared in change.md's table ("spec-planner.md + CLAUDE.md design-truth rules") — in scope, not flagged.

All `src/**` and `specs/capabilities/**`/`specs/design/**`/`specs/reference/**` edits map cleanly to the declared restyle/doc-source work; no domain/store files touched.

## Other findings

- `specs/design/prototype.html` comment changes around `.cbt-note` visibility ("always visible when a note is set" instead of "collapse-gated") are a doc-only correction — cross-checked against `src/lib/components/app/CombatantRow.svelte` (lines 226-229), which already renders the read-only note line whenever collapsed and non-empty, unchanged by this diff. This is a stale-doc fix, not a behavior change, but it's also not itemized in change.md's table — minor, low-risk, not blocking.
- Non-`en` locale files (`de`/`es`/`fr`/`ja`/`ru`) received the same new keys with English placeholder text, consistent with this repo's existing pattern of English stand-ins for untranslated locales (not new drift introduced by this unit).
- No regressions found in the store-seam invariant (`$state.snapshot()` usage unaffected — no Dexie-adjacent code touched).
