# Verification: 010-route-error-boundary

| AC | Verdict | Evidence |
|----|---------|----------|
| PLT-10 — `+error.svelte` exists, renders inside app shell with app tokens/styling (not SvelteKit default) | PASS | `src/routes/+error.svelte:1-46` — uses `EmptyState`, `Button` (shadcn), `m[...]` i18n strings; renders inside `+layout.svelte`'s `AppShell` (outlet-only file, no `<html>`/layout markup of its own). |
| PLT-10 — thrown error in `/` load or `store.hydrate()`/Dexie surfaces the boundary; offers recovery action (reload/`/combats`); AppShell chrome remains | PASS | `src/routes/+error.svelte:8-19` defines `reload()` (`location.reload()`) and `goToCombats()` (`goto('/combats')`), wired to two `Button`s at lines 34-45. `src/routes/+page.ts:29-31` comment + code explicitly lets a genuine `store.hydrate()`/Dexie failure propagate ("a genuine `store.hydrate()`/Dexie failure is left to propagate so the `+error.svelte` boundary can surface it"). Chrome preserved because this file supplies only the error-route outlet, per its own header comment (`+error.svelte:1-5`). |
| CLS-5 — `/combats/<nonexistent-id>` renders not-found state with a visible, labelled control to `/combats` | PASS | `src/routes/combats/[id]/+page.svelte:159-165` — `{:else if !combat}` branch renders `m['combat.notFound.title']()` and a `Button` with `aria-label={m['combat.notFound.back']()}` calling `onclick={() => goto('/combats')}`. Keys present in all 6 locales, e.g. `messages/en.json`: `"combat.notFound.title": "Combat not found"`, `"combat.notFound.back": "Back to Combats"`. |
| PLT-11 — Back closes top-most open sheet (Add/Edit/Numpad) and stays on page; subsequent Back leaves page; no stray forward-history pollution | PASS | `src/routes/combats/[id]/+page.svelte:74-101` — `closeTopOverlay()` closes numpad, then edit, then add (priority order = "top-most"); `handlePopState()` (79-84) consumes the pushed history entry and calls `closeTopOverlay()` only if `overlayHistoryPushed`; two `$effect`s (86-89, 91-99) push a history entry when any overlay opens and call `history.back()` on a normal (non-popstate) close so the entry doesn't linger — matches "no stray forward entries after a normal close" per the inline PLT-11 comment at lines 68-72. |
| CLS-7 — first-launch seed with no combat lands on `/combats`, no unhandled error; normal first-launch behaviour unchanged | PASS | `src/routes/+page.ts:24-31` — `wasFirstLaunch` branch checks `store.combats.length > 0 ? store.combats[0] : undefined`; if `seededCombat` exists it redirects to `/combats/${seededCombat.id}` (line 26, normal-path preserved); otherwise falls through past the `if` block to the unconditional `redirect(307, '/combats')` at line 32 — no throw, deterministic landing on Combats home. |

## Scope check
In-scope-only. Files touched: `messages/{en,de,es,fr,ja,ru}.json` (Phase 1 — new `combat.notFound.*` and `appError.*` keys, all 6 locales carry real translations, not English placeholders), `src/routes/+error.svelte` (Phase 2, new file), `src/routes/combats/[id]/+page.svelte` (Phase 3), `src/routes/+page.ts` (Phase 4), plus `specs/changes/010-route-error-boundary/change.md` (status flip draft→verifying) and `specs/backlog.md` (B-026 row: idea→in-unit, unit pointer added). No edits found in `specs/capabilities/*.md` (correctly deferred to doc-syncer per `tasks.md`'s scope guard), no domain-logic files (HP/initiative/turns/conditions/undo-redo untouched), no new routes, no `load`-function data-fetching restructuring, no `ColorTagDot`/search/reorder edits in the combats list. File ownership matches `tasks.md` phase boundaries exactly — no cross-phase file touches.

## Other findings
- `npm run gate` (lint + check + test:unit + build) passes clean on the working tree as-is — no regressions surfaced.
- Minor: `src/routes/+page.ts:20` still does `if (!store.ready) { await store.hydrate(); }` — a hydrate error genuinely propagates unhandled here (as intended, per AC), but this also means if `store.hydrate()` throws, the `wasFirstLaunch`/redirect logic below never runs; this is correct per the AC design (let it fail to `+error.svelte`) and not a defect.
- No regression spotted in the existing `!store.ready` loading branch (`+page.svelte:157-158`) or normal combat render path — left untouched as required.
