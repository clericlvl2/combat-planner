# Verification: 020-ux-interaction-overhaul

Pass type: code-diff (working tree vs HEAD).

| AC | Verdict | Evidence |
|----|---------|----------|
| SET-1: language switch, no reload | PASS | `src/routes/settings/+page.svelte:39` `setLocale(locale, { reload: false })`; `src/routes/+layout.svelte:29-33` `{#key store.settings.language}` wraps `AppShell` so every `m[...]()` call re-renders without `location.reload()` |
| SET-1: first-run default = English | PASS | `vite.config.ts` diff: `strategy: ['localStorage', 'preferredLanguage', 'baseLocale']` → `['localStorage', 'baseLocale']` (browser `preferredLanguage` autodetect removed) |
| PLT-2: `--content-max: 768px` token wired | PASS | `specs/design/tokens.css` `--content-max: 768px`; `src/routes/layout.css` `@theme inline { --content-max: var(--content-max); }` and `@utility content-container { max-width: var(--content-max); margin-inline: auto; padding-inline: var(--space-4); }` |
| PLT-2/PLT-3: combat header not clipped, shares container | PASS | `src/routes/combats/[id]/+page.svelte`: `<main class="content-container ...">`; `CombatHeader.svelte`: `<div class="content-container flex w-full items-center gap-2">` inside `<header>` |
| PLT-2: gutter padding so content never touches edges | PASS | `layout.css` `@utility content-container` includes `padding-inline: var(--space-4)` |
| PLT-3: page controls before nav group, nav visually distinct | PASS | `AppHeader.svelte`: `{#if headerAction.current}...{/if}` then `<DesktopNav />`; `CombatHeader.svelte`: Add/Start/Advance buttons then Overflow menu then `<DesktopNav />`; `DesktopNav.svelte`: `<nav class="... border-l border-border pl-2 lg:flex">` (divider = distinct section) |
| PLT-3: Combats-home "New combat" before nav | PASS | `src/routes/combats/+page.svelte` sets `headerAction.set(createHeaderButton)`; `AppHeader.svelte` renders `headerAction.current` before `<DesktopNav />` |
| PLT-3: single shared nav component, no duplicated markup | PASS | `src/lib/components/app/DesktopNav.svelte` (new file) consumed by both `AppHeader.svelte` and `CombatHeader.svelte`; no second `.nav-desktop`-equivalent markup block found in either |
| CLS-1: whole-card hover on desktop | PASS | `CombatRow.svelte`: `<Card ... class="... hover:bg-muted ...">` applied to the entire row `Card` |
| CLS-5: whole card opens, drag handle + ⋮ excluded | PASS | `CombatRow.svelte` `handleCardClick` checks `target.closest('[data-no-open]')`; grip span and `⋮` menu wrapper both carry `data-no-open` |
| CLS-6: drag only from grip handle | PASS | `CombatRow.svelte`: `<span use:dragHandle data-no-open ...>` wraps only the `GripVertical` icon; `CombatList.svelte` uses `dragHandleZone` (requires the paired `dragHandle` action to start a drag) |
| CLS-9: search matches title+description, highlighted | PASS | `src/routes/combats/+page.svelte` filter: `c.title...includes(q) || c.description...includes(q)`; `CombatRow.svelte` `highlightParts()` applied to both `titleParts` and `descriptionParts`, rendered via `<mark>` |
| CLS-9: highlight square corners | PASS | `CombatRow.svelte`: `<mark class="bg-primary/25 text-inherit">` — no `rounded-*` class present |
| CLS-2: Cancel inside dialog footer | PASS | `CombatFormDialog.svelte`: Cancel `<Button>` is a child of `<DialogFooter>`, itself inside `<DialogContent>` |
| CLS-2: color-tag tiles keep hover + persistent selected indicator, renders visibly | PASS | `ColorSwatchPicker.svelte`: `ToggleGroupItem` class has `hover:opacity-80` plus `data-[state=on]:ring-2 data-[state=on]:ring-offset-2 ... data-[state=on]:ring-foreground` — a real Tailwind utility class compiled to a visible ring, not a no-op; distinct from the hover-only opacity change |
| CLS-2: title/description placeholders are i18n keys | PASS | `CombatFormDialog.svelte`: `placeholder={m['forms.field.title.placeholder']()}` / `m['forms.field.description.placeholder']()`; keys present in `messages/en.json:96,98` and mirrored in all 6 locale files (`git diff --stat -- messages/` shows +5 lines in each of the 6 files) |
| CBT-2: chevron next to ⋮, not beside name | PASS | `CombatantRow.svelte`: Row 1 has `<span>{combatant.name}</span>` then a separate trailing `<div class="flex shrink-0 items-center gap-0.5">` containing `CollapsibleTrigger` (chevron) immediately followed by the `DropdownMenu` (⋮) |
| HP-4: unified HP number + health bar target | PASS | `CombatantRow.svelte`: single `<button type="button" class="flex min-h-11 w-full cursor-pointer items-center gap-3 rounded-md px-1 py-1 hover:bg-muted" onclick={() => onOpenNumpad(...)}>` wraps both the HP number span and `<HealthBar>` — no gap between two separate targets, one rounded hover area, `cursor-pointer` via native button default + explicit class |
| HP-6: desktop numpad = centered modal; mobile = drawer | PASS | `NumpadSheet.svelte`: `const isDesktop = new MediaQuery('(min-width: 1024px)')`; `{#if isDesktop.current}<Dialog>...{:else}<Drawer>...{/if}` |
| INI-2: hover scoped to inner control, fixed width, pointer, no min-height | PASS | `InitCell.svelte` diff: outer `Button` class changed from `h-auto min-h-11 min-w-11 rounded-full p-1.5` (had `min-h-11`) to `h-auto w-[4.5rem] cursor-pointer rounded-full bg-transparent p-1.5 hover:bg-transparent dark:hover:bg-transparent` (no `min-h`, fixed `w-[4.5rem]`, `cursor-pointer`); inner `<span class="chip init-pill ... group-hover:bg-muted group-hover:text-foreground">` carries the actual hover paint, confined to the pill |
| CBT-2: chevron and ⋮ render at same size | PASS | `CombatantRow.svelte`: both `CollapsibleTrigger`'s inner `Button` and the `DropdownMenuTrigger`'s inner `Button` use identical `class="size-11 lg:size-8"` |
| CBT-2: card container internal gap removed | PASS | `CombatantRow.svelte` diff: `<div class="flex flex-col gap-[var(--card-gap)]">` → `<div class="flex flex-col">` (no gap utility) |
| CBT-2: mobile ⋮ overflow trigger ≥44px | PASS | `CombatantRow.svelte`: overflow `Button` class `size-11 lg:size-8` — 44px (`size-11` = 2.75rem = 44px under this project's 4px spacing base) on mobile, shrinks only at `lg:` |
| CBT-3: Cancel inside dialog footer | PASS | `CombatantForm.svelte`: Cancel `<Button>` is inside `<DialogFooter>` inside `<DialogContent>` |
| CBT-3: numeric fields pre-filled as real values (10/0/10/10/10) | PASS | `CombatantForm.svelte` `$effect`: add-mode branch sets `initiativeBonus = 0; maxHp = 10; ac = 10; pd = 10; md = 10;` as bound `value`, not placeholder; confirmed by updated `CombatantForm.svelte.spec.ts` asserting `toHaveValue('10')` etc. |
| CBT-3: type-specific name placeholder + empty-name→real name fallback | PASS | `CombatantForm.svelte`: `namePlaceholder` derived per `type` from `forms.field.name.placeholder.{pc,ally,enemy}`; `submit()`: `const resolvedName = name.trim().length > 0 ? name : namePlaceholder;`; `messages/en.json:75-77` = "Hero Name"/"Ally"/"Enemy"; spec test asserts `onSubmit` called with `name: m['forms.field.name.placeholder.enemy']()` on empty submit |
| CBT-3: inline label-left grid, no multi-column AC/PD/MD grid | PASS | `CombatantForm.svelte`: every field row uses `grid grid-cols-[6rem_1fr] items-center gap-x-3` (Name, Type, Note) or `NumberField ... inline` (Max HP/Init/AC/PD/MD/Init-value), each of which renders the same `grid-cols-[6rem_1fr]` inside `NumberField.svelte`; no `grid-cols-3`/multi-column AC-PD-MD wrapper present anywhere in the file |
| CBT-3: modal fits phone-height viewport | PASS (structural only) | Row-per-field layout (vs. prior 3-column stat grid) and unchanged `gap-3` form spacing structurally reduce total height; actual phone-viewport fit (~667px) cannot be confirmed by static source read — no rendered/measured evidence available in this pass |
| CBT-3: placeholders are i18n keys | PASS | `CombatantForm.svelte`: `placeholder={m['forms.field.note.placeholder']()}`; namePlaceholder as above; all present in `messages/en.json` |
| NumberField: opt-in inline variant, default unchanged | PASS | `NumberField.svelte`: new `inline = false` prop; template `<div class={inline ? 'grid grid-cols-[6rem_1fr] items-center gap-x-3' : 'flex flex-col gap-1'}>`; `CombatHeader.svelte`'s round/escalation `NumberField` calls omit `inline`, so they keep the `flex flex-col` stacked layout |
| SET-5: stray colored border removed from About box | PASS | `src/routes/about/+page.svelte`: privacy box is `class="rounded-lg border border-border bg-card p-3 text-sm"` — plain `border-border`, no accent/colored border-tint class present |
| PLT (mobile): bottom padding > 2×FAB height + gaps | PASS | `src/routes/combats/[id]/+page.svelte`: outer wrapper `pb-56 lg:pb-8` (224px); two FABs are `size-14` (56px) at `bottom-4`/`bottom-24` → 2×56 + 24px gap = 136px < 224px |
| PLT (mobile): header ⋮/icon controls ≥44px | PASS | `CombatHeader.svelte`: back/overflow buttons `class="size-11 shrink-0 lg:size-8"` (44px on mobile) |
| PLT: combat header full-bleed, no gutter leak | PASS | `AppShell.svelte`: `routeHasOwnHeader` branch renders `{@render children()}` directly (no `.content-container` wrapper) for `/combats/[id]`; `CombatHeader.svelte`'s `<header>` itself has no `content-container` class (only its inner `<div>` does), so the header's `bg-card` background spans the full viewport while content is capped/centered inside |
| Design-source: `.jump-turn` removed | PASS | `grep -n "jump-turn" specs/design/prototype.html specs/design/card-prototype.html` → no matches |
| Design-source: prototype/inventory reconciled (768px, header sectioning, chevron, numpad-modal, HP target, init fixed-width) | PASS | `specs/design/prototype.html` diff: `--desktop-w: 1024px` → `768px`, `.chip.init-pill` width rule added, HP-unified-target comment added; `specs/reference/component-inventory.md` diff adds "Desktop content container" section, rewrites "Navigation placement per breakpoint" (control-then-nav, single `DesktopNav`), Combatant-card Row 1/2/3 (chevron cluster, unified HP target, INI-2 fixed-width/no-min-height), Numpad-sheet section (desktop modal), Combats-list-row section (CLS-1/5/6/9) |
| Whole unit: `npm run gate` passes | PASS | `npm run gate` run in this pass: lint/check/test/build all completed, exit code 0, `build` succeeded writing to `build/` with no errors reported |

## Scope check

In-scope only. `git status --porcelain` shows edits confined to: the 6 `messages/*.json` locale files, `specs/design/{prototype,card-prototype,tokens}.css/.html`, `specs/reference/component-inventory.md`, the `src/lib/components/app/*` files named in change.md's "What changes" table (+ new `DesktopNav.svelte`), `src/routes/{+layout,about/+page,combats/+page,combats/[id]/+page}.svelte`, `src/routes/layout.css`, and `vite.config.ts` (the SET-1 locale-strategy line, explicitly declared in-scope). No Dexie schema/migration file, no `src/lib/stores/domain/constants.ts` numeric-range file, and no unrelated capability files were touched.

## Other findings

- **Capability spec not updated to match shipped CBT-3 behavior (contradiction, not on this unit's own checklist).** `specs/capabilities/combatants.md` was **not modified** by this diff (`git diff -- specs/capabilities/combatants.md` is empty), yet it still states, under CBT-3's AC: *"Submitting the add form with an empty or whitespace-only name is rejected."* The shipped code in `CombatantForm.svelte` (and the rewritten `CombatantForm.svelte.spec.ts` test, "empty name does not block submit") now does the opposite — an empty name is *accepted* and silently replaced by the type placeholder as the real stored name. This is a real, currently-live contradiction between the capability spec (the canonical AC owner per this repo's doc rules) and the actual behavior; it is out of this change unit's declared design-source-only doc list (`prototype.html`/`card-prototype.html`/`component-inventory.md`), but it should be flagged for a follow-up doc-sync pass before this unit is considered fully closed, since `combatants.md` is the stated single-owner file for CBT-3's AC text.
- Leftover unused i18n key: `messages/en.json:74` still has `"forms.field.name.placeholder": "Namius Name"` (the old single placeholder, pre-CBT-3c), now dead — not referenced anywhere in `CombatantForm.svelte` (which uses the three type-specific keys instead). Harmless but worth a cleanup note.
- `AppHeader.svelte` still computes its own local `links`/`isCurrent` (duplicated logic, not markup) purely to derive the page `<title>` text — this doesn't violate the "no duplicated `.nav-desktop` markup" AC (the actual nav row is only rendered once, by `DesktopNav`), but it is a small duplication of the link-list data that a future pass could hoist to a shared source.

## Summary

34 ACs checked: 33 PASS, 1 PASS-with-caveat (phone-viewport fit — structural only, not visually confirmed), 0 FAIL. Scope respected. One noteworthy contradiction found outside the checklist: `specs/capabilities/combatants.md`'s CBT-3 AC text ("empty name rejected") is now stale relative to shipped code and needs a doc-sync follow-up.
