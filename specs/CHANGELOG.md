# Changelog

Append-only changelog for Combat Planner spec-driven development. `/spec-close` adds one row
per closed change unit.

## Change units

| Unit | Date | Change |
|---|---|---|
| 001-m2-component-tests-and-dogfood | 2026-07-03 | 7 new `vitest-browser-svelte` component test files for M2 UI components (CombatantRow, CombatantForm, HealthBar, NumpadSheet, ConditionPicker, InitCell, CombatHeader); DM dogfood attestation recorded, referencing pre-existing first-touch play sessions already folded into the M2 rework (`dfc8582` + `82da34a`); B-001/B-002 flipped to `done`. |
| 002-combats-home | 2026-07-04 | M3 Combats home shipped (CLS-1..7): list render with color-tag rows, create with 100-cap block, edit (new `editCombat` domain fn + `CombatStore.editCombat`), confirm-gated cascade delete (no undo), tap-to-open navigation, drag reorder persisting `listOrder`, and first-launch routing branch. Row `⋮` menu ships Edit + Delete only; Export/share stays deferred to CLS-8/M5. |
| 003-ui-design-prototype | 2026-07-06 | Unit A of the 5-unit design chain: delivered `specs/design/prototype.html`, a single self-contained (inline-only) design prototype covering all core screens + modals × mobile/desktop frames × dark/light themes, with a WCAG-AA contrast note; demonstrates PLT-2/PLT-3/PLT-5. No capability/reference spec edits (reconciliation deferred to unit B); gate stays green. |
| 004-design-feedback-iteration | 2026-07-09 | Unit B of the 5-unit design chain: `specs/design/prototype.html` iterated through review rounds R1–R6 to a locked, converged design (Combats-home search/icon-nav/1-col desktop, combat-card restyle via new `specs/design/card-prototype.html`, Setup two-FAB + Active `round-esc-bar` chrome, Numpad-sheet redesign); `specs/reference/component-inventory.md` and `specs/capabilities/platform.md` (PLT-2/3/5) reconciled both to shipped M2 code (closing the first-touch doc-sync miss) and to the converged prototype target; passed an independent doc-pass verify. |
| 005-design-tokens | 2026-07-09 | Unit C of the 5-unit design chain: `src/routes/layout.css`'s STUB token block replaced with the real, WCAG-AA token set (dark + light) — neutral chrome, type colors, health bands, 8 ADR-012 color-tag swatches, typography, spacing, radius, elevation — token values verified verbatim-equal to the approved `specs/design/prototype.html`; every semantic fg/bg pair meets WCAG-AA in both themes (PLT-5 satisfied). No capability/reference wording change needed — PLT-5 already stated the requirement generically. |
| 006-combats-list-screen | 2026-07-09 | Unit D of the 5-unit design chain: restyled CombatList/CombatRow/ColorTagDot/CombatRowMenu/EmptyState (CLS-1, CLS-6), CombatFormDialog (CLS-2/3), and ConfirmDialog (CLS-4) to the approved template; new AppShell/AppHeader/NavSidebar chrome with per-breakpoint nav (swipe-right sidebar mobile, burger tablet, inline icon-nav desktop) and a desktop header create-button / mobile FAB split, incl. the `header-action.svelte.ts` seam (PLT-2/3); restyled Settings (as three inline `<section>` groups, not dedicated sub-components) and About routes, with Settings gaining its first real language/theme/reset-all UI wiring (SET-1/2/3) — accepted as in-scope since the pre-change route was a bare stub with no prior behavior to hold identical, though it ships with no test file yet (flagged as a gap in `specs/reference/acceptance-matrix.md`). `specs/reference/component-inventory.md` corrected: the pre-written Settings/About sub-component names (SettingsGroup/LanguageSwitcher/ThemeSwitcher/DataActions/AboutPage) didn't ship as separate components, and the SearchBar note's "real filtering is unit D" promise didn't ship either. |
| 007-combat-screen | 2026-07-09 | Unit E of the 5-unit design chain: restyled the Combat screen (CombatHeader/RoundEscBar, CombatantRow's 5-row card, NumpadSheet, CombatantForm) to the approved template and unit-005 tokens, plus a new `JumpToTurnButton.svelte`; no game-mechanics change (LIF/INI/TRE/HP/LOG/CND/CBT/UND logic verified identical). Closed two shipped-code gaps flagged in `specs/reference/component-inventory.md`: the combatant note now renders read-only under the card in both collapsed and expanded state (was expanded-only), and `JumpToTurnButton` now ships. Also removed the health-band card-background tint — HealthBar fill colour (plus band width and `aria-label`) is now the sole HP-status visual signal, reconciled into `specs/capabilities/hp.md` HP-4, `specs/capabilities/combatants.md` CBT-2, and `specs/reference/component-inventory.md`. |
| 008-deploy-vercel | 2026-07-09 | Production build deployed and reachable: `vercel.json` pins `buildCommand`/`outputDirectory: "build"`/`framework: null` so Vercel serves the `adapter-static` bundle (ADR-007); live at `https://combat-planner-five.vercel.app/` (HTTPS, 200, app-shell HTML), with Vercel Git integration auto-deploying pushes to `main`. New `specs/capabilities/platform.md` requirement PLT-9. |
| 009-visual-fidelity-cure | 2026-07-10 | Visual-fidelity cure for units 006/007: theme applied at app root (`src/lib/theme.ts` + root `$effect`, persists on reload), typography scale wired into Tailwind `--text-*` tokens, shadcn shadow aliases (`--shadow-sm`/`--shadow-md`), combatant-card recipe restyled (12px radius, token border, 6px type stripes w/ gap, active-turn `▶` dot, 24px init chip w/ ≥44px target, visible 8px health-bar track, offset temp-HP badge, bold def-stat values), desktop (≥1024px) Active-header Advance/Jump roundels + Round/Escalation label-value bar, primary Setup Start FAB, NumpadSheet commit-tint AA fix (both themes) + collapsible History w/ chevron, combats-list untitled-combat placeholder + new `SearchField` component (view-local `$derived` title filter, shown only when list non-empty). Affected: SET, PLT-3, PLT-5, CBT, TRE, HP, CLS (new CLS-9 — Search), LIF, plus cross-cutting typography/shadow tokens; `specs/reference/component-inventory.md` and `specs/reference/i18n-catalog.md` updated; `card-prototype.html` light-theme health/combat hexes corrected to match `prototype.html`. |
| 010-route-error-boundary | 2026-07-11 | Route-level error boundary added (`src/routes/+error.svelte`, new `platform.md` PLT-10): a thrown error in the `/` load or in `store.hydrate()`/Dexie access now renders inside the app shell with a reload/`/combats` recovery action, never SvelteKit's default error page. Back-button on the combat screen now closes the top-most open Add/Edit/Numpad sheet instead of leaving the route (new PLT-11). Opening a stale/nonexistent combat id shows a styled not-found state with a labelled control back to `/combats` (CLS-5 amended). First-launch seeding that yields no combat now lands deterministically on `/combats` without throwing (CLS-7 amended). Promotes B-026. |
| 011-remove-jump-to-turn | 2026-07-11 | Removed the manual "jump to turn" control (mobile Jump pill, desktop `header-jump` roundel, `JumpToTurnButton.svelte`, `jumpToActiveTurn` handler, `jump`/`Crosshair` icon, `active.jumpToTurn`/`a11y.jumpToTurn` i18n keys) — it duplicated auto-scroll-on-advance. Wired the auto-scroll-on-advance behavior TRE-2 already specified but never had code for into the advance flow, so it's now real and Jump-free. `TRE-2` and `PLT-3` amended to drop the removed control; `specs/reference/component-inventory.md` and `specs/reference/i18n-catalog.md` re-synced. Promotes B-022. |
| 012-numberfield-input-fixes | 2026-07-11 | NumberField numeric input reworked — mobile-broken negative entry + desktop native-spinner overlap fixed (type=number→text, minus-capable inputmode, regex sanitize, role=spinbutton restored), digit-cap paste/programmatic bypass reaches clamp (closes B-013), stepper −/+ buttons gain aria-labels from new `a11y.numField.decrease`/`increase` keys (6 locales). Touched: `src/lib/components/app/NumberField.svelte`, new `NumberField.svelte.spec.ts`, `CombatantForm.svelte.spec.ts`, `messages/*.json` (6), `specs/reference/i18n-catalog.md`. |
| 014-unified-design-tokens | 2026-07-14 | New `specs/design/tokens.css` is now the single source of truth for every raw design token (colors, `--space-*`, `--font-*`, radius scale, and the component dims `--card-pad`/`--card-gap`/`--card-border`/`--hp-size`/`--badge-width`/`--touch-min`), `@import`ed verbatim by `prototype.html`, `card-prototype.html`, and `src/routes/layout.css` — closes the prior hand-copied drift. Theming moved from a `.dark` class to a `[data-theme="light"\|"dark"]` attribute (`theme.ts`, `@custom-variant dark` remapped), so additional themes are now a token-block addition, not a code change (SET-2 amended). One documented value exception: `--card-pad`/`--hp-size` synced to the shipped 12px/18px render (prototype source updated to match). WCAG-AA recomputed from actual hex surfaced pre-existing light-theme gaps (`--text-faint`, `--health-wounded`/`--health-bloodied`, 5 combat swatches as small text) — documented in PLT-5 and deferred to backlog B-028, not fixed in this unit. Promotes B-027. |
| 013-localization-full-fix | 2026-07-14 | Full localization fix: all 5 non-English locales (`de/ru/es/fr/ja`) rewritten from ~95% English stubs to real native translations, `_translation_status` STUB flag removed from all 6 `messages/*.json` files, 5 previously-missing keys added and translated in every locale, and all 6 locales now share an identical 183-key set with ICU params/plural branches preserved. New `a11y.close` Paraglide key backs the sr-only dialog/sheet close control (`dialog-content.svelte`, `sheet-content.svelte`), replacing hardcoded `"Close"` text so it localizes with the chosen locale. `specs/reference/i18n-catalog.md` synced in-unit (rows for 7 shipped keys + `a11y.close`, 2 ICU-notation corrections). No capability-file wording change needed — SET-1/PLT-5 already stated the correct observable behavior; this unit brought the code into compliance. |
| 015-light-theme-aa-contrast | 2026-07-14 | Light-theme WCAG-AA contrast pass: darkened light `--health-wounded`/`--health-bloodied`, light combat swatches `blue`/`orange`/`green`/`neutral`/`teal`, and `--text-faint` (both themes) in `specs/design/tokens.css` to clear ≥4.5:1 normal-text AA — hue-preserving, luminance-only shifts (9 token hexes). `platform.md` PLT-5 restored to an unqualified both-themes-AA guarantee; closes backlog B-028. |
| 016-temp-hp-badge-doc-fix | 2026-07-14 | Doc-only fix: `specs/capabilities/combatants.md` CBT-2 corrected — the temp-HP badge was wrongly documented as expand-only; it's now documented as part of the compact row (visible whenever `tempHp > 0`, no expand needed), matching `CombatantRow.svelte` and `component-inventory.md` Row 2 all along. Expand-only prose/AC now names only the condition picker. No code changes. Closes backlog B-012. |
| 017-repoint-dead-doc-citations | 2026-07-15 | Repointed all dead pre-migration `§` doc-section citations (UX/Component Inventory/Data Model/Data/Rules/Test Plan/Glossary namespaces, archive deleted 2026-07-03) in `src/**` comments/JSDoc/test-`describe` labels to live capability IDs or `specs/reference/*.md` filenames, dropping where no clean target existed; also fixed `NumpadSheet.svelte`'s `§7` comment (→ `limits.md`) and reconciled `src/lib/components/app/README.md`'s stale component map (dropped ~15 never-built phantom rows, repointed all bare `§N`/`UX & IA §` to `component-inventory.md`/capability IDs); no capability/reference wording change needed — comment/doc-pointer-only, zero runtime behavior change; promotes B-016. |
| 018-routing-fallback-not-found | 2026-07-15 | Routing: Vercel SPA deep-link fallback + styled not-found for unknown paths. `vercel.json` gains a catch-all rewrite (`/(.*)` → `/index.html`, applied after the filesystem check) so deep links/hard reloads to client-only paths boot the app shell instead of hitting Vercel's own 404 (`platform.md` PLT-9 amended). New root `src/routes/[...catchall]/+page.svelte` renders a styled not-found screen with a visible + `aria-label`-labelled Back-to-Combats control for paths with no route match at all — never shadows real routes, never intercepts thrown errors (new `platform.md` PLT-12). New i18n keys `route.notFound.title`/`route.notFound.back` in all 6 locales. CLS-5 (bad combat id) and PLT-10 (thrown-error boundary) unchanged. |
| 019-ui-fidelity-grand-revision | 2026-07-15 | Cross-screen fidelity pass reconciling running app and design prototypes both directions (14-agent audit, promotes B-021): combatant card renders exactly one type-stripe per type (CBT-1, `typeStripeCount` fix); add-mode combatant/combat-create submit buttons now read "Add"/"Create" vs. edit-mode "Save" (CBT-3, CLS-2, new `forms.action.create`/`forms.action.add` keys); combats-list empty state now renders an icon + description + "New combat" CTA (CLS-1, new `combats.empty.description`); create-combat modal title/cap-error-banner-reset bugs fixed (CLS-2); reset-all confirm copy corrected to state combats are deleted and language/theme are kept, Settings data row gains a visible caveat (SET-3, `dialogs.resetAll.body` rewritten, new `settings.data.resetAll.caveat`); desktop Settings/About/combats-list nav is now reachable from an open combat screen via `CombatHeader`'s own desktop icon-nav row (PLT-2); Setup "Start" is now a play-glyph icon-roundel/FAB instead of a text pill (LIF-2). Numerous presentation-only fidelity fixes (typography, spacing, radii, touch-targets) across CBT-2/3/5, INI-2/3, HP-4, CND-3, TRE-3, PLT-5 land in code/design-source only — already-accurate capability/reference wording left untouched. `specs/reference/i18n-catalog.md` re-synced for all new/changed keys. `npm run gate` green. |
| 020-ux-interaction-overhaul | 2026-07-16 | Interaction/layout overhaul across three dogfood rounds (33/33 ACs verified): language switch now applies instantly with no reload and first-run default is always English, dropping browser-locale autodetect (SET-1); new app-wide 768px `--content-max` desktop container with gutter padding, combat-screen header is a true full-bleed bar and no longer clipped, header control-then-nav sectioning via a single shared `DesktopNav`, mobile bottom safe-area now exceeds 2×FAB height, header overflow/icon controls ≥44px on mobile (PLT-2, PLT-3); combats-list rows are whole-card hover/click-to-open with drag reserved to the grip handle, search now matches title+description with a square-corner highlight, create-combat modal Cancel moved inside the footer, color-tag swatches are buttons with a persistent visible selected ring, placeholders are i18n keys (CLS-1, CLS-2, CLS-5, CLS-6, CLS-9); combatant-card expand chevron moved next to `⋮` (equal size), card internal gap removed, mobile `⋮` ≥44px (CBT-2); add/edit-combatant modal Cancel moved inside the footer, Name is now optional with a type-specific placeholder ("Hero Name"/"Enemy"/"Ally") that becomes the real stored name when left empty, numeric fields pre-fill real editable defaults (10/0/10/10/10), and the modal is relaid as an inline label-left grid with no multi-column AC/PD/MD grid (CBT-3 — corrects the stale "empty name rejected" AC to match shipped behavior); current-HP number + health bar are a single unified rounded tap target, desktop numpad is a centered modal vs. mobile drawer (HP-4, HP-6); init cell hover is scoped to the inner control with a fixed width, pointer cursor, and no `min-height` (INI-2). New i18n keys (`forms.field.title.placeholder`, `forms.field.description.placeholder`, `forms.field.name.placeholder.{pc,enemy,ally}`) added across all 6 locales; `forms.field.name.placeholder`/`errors.nameRequired` are now dead keys, left in place. `specs/reference/limits.md` and `specs/reference/i18n-catalog.md` updated to match. `npm run gate` green. |
| 021-combatant-modal-and-header-fix | 2026-07-16 | Reopened dogfooding pass — grew from the original two-fix scope (combatant modal overflow + header cap) into an app-wide modal/header/card/control polish sweep. Header/gutter: `AppHeader`/`CombatHeader` drop `px-3`, `.content-container`/`.content-container-wide` gutter narrowed to `--space-3` (12px), the combat header's back-link chevron sits flush to the content edge (spacer div, no `flex-1`), Round/Escalation triggers and the shared `Button` base gain `cursor-pointer` (PLT-2). `CombatantForm`, `CombatFormDialog`, and `ConditionPicker` each split into a centered Dialog (≥1024px) / bottom Drawer (mobile) via `MediaQuery`, and every mobile Drawer (plus the pre-existing Numpad sheet) gets a real swipe-to-close handle (`DrawerPrimitive.Handle`) (CBT-3, CLS-2, CND). Combatant-form fields are stacked with 2-across `NumberField` pairs in a new order — `[Max HP\|AC]`, `[PD\|MD]`, `[Init Bonus\|Initiative]`, Note last — the manual Initiative field now renders unconditionally in add and edit (amends INI-3), and the Type selector is now per-type-colored pills, not a neutral segmented fill (CBT-3, CBT-4, INI). Shared `NumberField` resized to 40px with a red-ring/`aria-invalid`-only invalid state (inline clamp-hint text removed) and `lg:`-gated focus rings; `CombatFormDialog`'s combat-cap block is now silent (no cap-error text); `ColorSwatchPicker` is now a single full-width stretched row of equal-width swatches. `CombatantRow`'s type-color stripe is replaced by a small leading color dot (decorative, no compensating label — amends PLT-5) (CBT-2). Mobile FABs suppress their focus ring below 1024px. Dead `typeStripeCount` export and orphaned i18n keys `a11y.typeBadge`/`errors.clamp`/`errors.combatCap` removed (6 locales, Paraglide regenerated). `specs/reference/component-inventory.md` and `specs/reference/i18n-catalog.md` re-synced. `npm run gate` green. |
| 022-settings-menu-form-polish | 2026-07-17 | Five bundled dogfood-surfaced polish fixes: Settings theme control is now a `Select` dropdown matching the SET-1 language selector's shape, replacing the three-button `ToggleGroup` (SET-2); the About link row removed from Settings, `/about` and nav destination unchanged (SET-5); creating a combat with a blank/whitespace title now persists a localized default title (new key `combats.defaultTitle`, "New combat") instead of an empty string, Create path only — the "Untitled combat" placeholder (CLS-1) now only surfaces for non-Create paths to blank (CLS-2); form fields suppress their focus ring for pointer/touch focus (`:focus:not(:focus-visible)`) while keyboard `Tab` keeps the visible ring on all viewports, and the three `⋮` `DropdownMenu`s (`CombatRowMenu`, `CombatantRow`, `CombatHeader`) get ≥44px-tall menu items via the shared `DropdownMenuItem` (PLT-2, PLT-5 note preserving keyboard-focus a11y). `specs/reference/i18n-catalog.md` gains `combats.defaultTitle`. `npm run gate` green. |
| 023-condition-picker-pill-restyle | 2026-07-17 | `ConditionPicker.svelte`'s selected toggle-pill state restyled to a soft ~15% `--primary` tinted fill + primary border + default foreground text, replacing the prior heavy solid `bg-primary`/`text-primary-foreground` fill; unselected outline pills, the Dialog/Drawer split, `min-h-11` touch height, and toggle/aria behavior all unchanged (CND-3). |

## Spec era (rounds 1–13)

Frozen — the spec was hardened in numbered rounds (audit / `/grill-me` passes) before the
2026-07-03 spec-driven-development migration; kept verbatim for provenance.

| Round | Date | Change |
|---|---|---|
| 1 | — | Initial spec set: PRD, Rules & Glossary, Data Model, UX & IA. |
| 2 | 2026-06-27 | Setup→Active lifecycle; temp-HP / heal model; 30/100 caps; (then) per-action undo toasts. |
| 3 | 2026-06-28 | `ally` type (visual-only); Overview hub created; round-99 wrap-only block + minor re-audit fixes. |
| 4 | 2026-06-28 | Undo toasts → per-combat **Undo/Redo history** (10-deep); full **control-surface map** (now `specs/reference/component-inventory.md` + `specs/capabilities/platform.md` PLT-2/PLT-3). |
| — | 2026-06-28 | **Architecture ADR** landed — SvelteKit / Dexie / PWA / Paraglide / etc. |
| 5 | 2026-06-29 | Per-combatant **HP change log** (now `specs/capabilities/hp-log.md`); separate from undo; rides export/import. |
| 6 | 2026-06-29 | Full audit → 8 fixes (Advance FAB, Import control, Max-HP undo step, Start snapshot + 4 minor); status split into this doc. |
| 7 | 2026-06-29 | Open Qs resolved — ADR-011 Lucide icons, ADR-012 color-tag palette, ADR-013 schema versioning. |
| 8 | 2026-06-29 | Consistency pass — dedup to one-fact-one-doc (limits → Rules §7, undo → Data §8, HP-log → Data §9, controls → UX §9); Overview Locked-scope cut + **Maintaining this set** added. |
| 9 | 2026-06-29 | **Component Inventory** doc added — component catalog + hierarchy + variant/prop matrix + component → primitive → glyph map; flags missing chrome glyphs back to ADR-011. |
| 10 | 2026-06-29 | **i18n Message Catalog** doc added (ADR-005) — English-source key tree + copy + ICU/interpolation; condition + a11y label strings; cap/clamp numbers pointed to Rules §7. |
| 11 | 2026-06-29 | **Test Plan** doc added (ADR-009) — unit/component/E2E layer split + case inventory + requirement→test acceptance matrix (PRD epics + UX F1–F5); numbers/transitions/flows pointed to their owner docs. |
| 12 | 2026-07-01 | **First-touch rework** — escalation override is now long-press (not tap), mirroring the init cell; the init cell locks (no tap-roll / long-press) once a combat is Active, manual edit only (add form too, for mid-combat latecomers). |
| 13 | 2026-07-02 | **First-touch rework II** — escalation die is a stored value (round-wrap-only +1, decoupled from the round counter, no more auto/override); `monster` → `enemy` rename everywhere (`DATA_VERSION` 1→2 migration); persistent per-row `⋮` menu replaces the expanded action row; native `min`/`max` on numeric inputs; initiative range −9..99, no more ± toggle; combatant type shown as color stripes (icon dropped, `aria-label` compensates — amends the "never color-alone" invariant); card background reflects HP status; full-width Save buttons; Setup no longer auto-sorts live (sorts on Start only). |
